# decisions.md — log local de MyProject7

- 2026-09-11 AK audio: extraida la pista AAC de Fab/0911 (CapCut 0911.mp4) a WAV mono 44.1kHz 0.743s, importada `/Game/Audio/Weapons/S_AK74M_Fire_0911`. Solo `DT_FPWeapons` fila AK74M (tecla1) FireSound apunta al SoundWave; FN502/Benelli y otras armas no se tocaron. Tabla reexportada y guardada; prueba auditiva en PIE pendiente. Como AK dispara cada0.1s, ajustar concurrencia si se solapa demasiado.

- 2026-09-11 AK: BP_Bullet_AK47.ColSphere SphereRadius48->3 con escala0.1 (radio efectivo4.8cm->0.3cm). Compila0/0 guardado. La colision era sobredimensionada; NO probado que explique desvio lateral ADS. Spawn Bullet trace verificado usa GetCameraLocation+GetCameraRotation finales. ADS en PIE FPWeaponIndex2 AK74M_Arms RelLoc(7.780463,-3.5,-21.923162), MuzAim(0,50,13). Calibracion visual y prueba impactos pendientes; no declarar resuelto.

- 2026-09-11 Abandoned_Courtyard suelo: MI_AC_MossGround copia MI_vcqhcig Tiling13 aplicado a AC_Ground.28 decals Military Dirt AC_GroundDirt_ dieron rectangulos blancos en PIE; ocultados en juego/editor y guardados recuperables. Mezcla NO terminada, revisar dominio/texturas/alpha del material importado antes de reactivar.

- 2026-09-11 laser/aim: BP_Player LaserDot tenia bHiddenInGame=true pero K usa SetVisibility; cambiado false. Spawn Bullet GetForwardVector D164D593 ahora recibe GetCameraRotation 7A9ECDDE (POV final), no ComponentRotation. Compila0/0 guardado. PENDIENTE pruebas K1/2 y HIP/ADS; no recalibrados los MuzHip/Aim. Abandoned_Courtyard:6 decals pack Mat_Graffiti_1..6 roll90 y neumáticos importados escalados4m y pivote compensado; provisional oculto.

- 2026-09-11 Abandoned_Courtyard: U 36x32m dos plantas, escaleras17cm y huecos de losa; M_AC_AgedBrick, suelo/cubiertas DirtyConcrete. Corregidos textos provisionales a banda solida Z352/354, tamano46/44 y jardinera de cruz a perimetro. Guardado CFA con guardia de mundo; captura revisada. PENDIENTE grafiti artistico, vegetacion natural, desgaste, iluminacion y recorrido jugable. No declarar ultrarrealista/aprobado. No tocar MainMenu/CQB.

Formato: una línea por arreglo. Qué problema → qué solución.

- Cuelgue duro al disparar "a lo largo del pasillo" en Backrooms_Level_0 (vertical OK): NO era bucle de Blueprint / proyectil / animación (todo verificado limpio). Era churn de la escena de ray tracing: cada bala lleva mesh "Sphere" con `bVisibleInRayTracing=true`, y en automático varias balas vivas a la vez (LifeSpan 3 s, al frente no chocan pronto) disparaban recreaciones completas del Persistent SBT ("Recreating Persistent SBTs ... NumGeometrySegments 0→512", ~70/sesión en el log) → stall del render thread de ~18 s que se recupera solo. Solución: `Sphere.bVisibleInRayTracing=false` en BP_Bullet_AK47, BP_Bullet, BP_Bullet1, BP_Bullet_Shotgun. Test de confirmación sin tocar assets: consola `r.RayTracing.Geometry.StaticMeshes 0` (o `r.RayTracing 0`) → si deja de colgar, era esto.
- Error "Accessed None" en UpdateFPWeaponHUD (HUD null al inicio): agregado chequeo IsValid(HUD) antes de SetText en UpdateFPWeaponHUD. No relacionado al congelamiento al disparar (ese fix de ray tracing en BP_Bullet* sigue intacto).
- AK74U/FN502 sin sonido de recarga tras borrar HK416: los chequeos FPWeaponIndex==2/3 en Reload (R) quedaron con indices viejos; corregidos a 1/2.
- Instalado AK-74M (paquete fps-ak-74m-animations, mesh combinado brazos+arma, esqueleto propio FpsAnims_Skeleton) en la tecla 1: `cfa import_asset` solo trae el mesh sin animaciones desde ese FBX (igual que el AKM) — animaciones se importan a mano con `AssetImportTask`+`FbxImportUI` (`import_animations=True`, `mesh_type_to_import=FBXIT_ANIMATION`, skeleton ya importado). Creado ABP_FPS_AK74M clonando el patrón de ABP_FPS_Benelli (login: `TryGetPawnOwner`→`Cast BP_Player`→leer Reloading/GetVelocity/LastFireTime; state machine Idle/Walk/Fire/Reload con 9 transiciones). AK74M queda como Index=2 en DT_FPWeapons (Benelli=1); extendida la función EquipFP (hide-chain + rama de boca de cañón) y agregado el binding IA_Weapon1→EquipFP(2). Boca de cañón (AK74M_MuzHip/MuzAim) puesta con un offset estimado (X=55,Y=5,Z=8 local a AK74M_Arms) — falta calibrar visualmente en PIE porque las sesiones PIE se cortan solas en este proyecto (no se pudo verificar disparo en vivo esta vez).
- Mapa CQB_Wood_Houses no tenía PlayerStart (por eso el spawn dependía de la posición de cámara del editor); se creó `PlayerStart_Spawn` en el nivel. Además, BP_Player guarda un `Transform` en el SaveGame "Slot 1" (BP_Save) y lo reaplica en BeginPlay, así que mover el PlayerStart NO alcanza si ya existe un save previo — hay que borrarlo con `unreal.GameplayStatics.delete_game_in_slot('Slot 1', 0)` (vía `cfa execute_python`) para que vuelva a usar el PlayerStart. Verificado con `--new-window` en `play_in_editor` (el modo de viewport activo puede arrastrar comportamientos raros de cámara, usar new-window para probar spawn real).
- AK74M sin textura: los materiales ak74m/arms (proyecto usa Substrate) daban error de compilación de shader "Sampler type is Linear Color, should be Color" en los TextureSample de Metallic/Roughness — al fallar el shader, cae al material gris por defecto. Corregido con `set_material_expression_property SamplerType=SAMPLERTYPE_Color` en esos 3 nodos (2 en ak74m, 1 en arms).
- AK74M sin disparo real (bala no salía): la fila "AK74M" en DT_FPWeapons tenía BulletClass/FireSound/DisplayName en blanco — `add_data_table_row`/`update_data_table_row` del CLI no logran escribir campos de tipo "class"/"object"/FText en este struct (se quedan en "None"/"" sin dar error). Solución que sí funciona: `dt.export_to_json_string()` para ver el formato exacto (`"/Script/Engine.BlueprintGeneratedClass'/path/Asset.Asset_C'"` con comillas simples), reconstruir el JSON completo con ambas filas, y `dt.fill_from_json_string(json)` vía `cfa execute_python`.
- AK74M: el disparo salía desatado a la izquierda del arma. La rotación del disparo NO usa la rotación del componente MuzHip/MuzAim (usa la rotación de cámara/control por separado, confirmado leyendo el `MakeTransform` que arma el `SpawnTransform`) — solo importa la LOCATION de MuzHip/MuzAim. El offset local que se le había puesto a mano (X=55,Y=5,Z=8) quedaba mal por la rotación Yaw=-89 del componente AK74M_Arms (convierte X-local en -Y-mundo). Recalibrado con la técnica de deproyección: tomar cámara world loc/forward/right/up, `target_world = cam_loc + fwd*58 + right*9 - up*11`, `unreal.MathLibrary.inverse_transform_location(AK74M_Arms.get_world_transform(), target_world)` para el nuevo RelativeLocation local. No se tocó rotación de MuzHip/MuzAim ni la posición/rotación del mesh del arma.
- AK74M: sonido de recarga viejo (SW_AK74U_Reload, heredado) desconectado por completo (se rompieron los 2 links entrantes al PlaySound2D dentro de "Reload (R)"; quedó código muerto, no se borró el nodo).
- AK74M: agregado fogonazo real (`P_AssaultRifle_MuzzleFlash`, ParticleSystem Cascade ya incluido en `/Game/MilitaryWeapDark/FX/`, diseñado para rifles de asalto) + luz puntual amarilla (`AK74M_MuzzleLight`, hija de AK74M_Arms, en la misma posición que MuzHip, Intensity 0 por defecto). En el evento "Fire Event" de BP_Player, tras "Set LastFireTime", se agregó un Sequence en paralelo (sin tocar la rama original) que en cada disparo: `SpawnEmitterAtLocation` en la ubicación/rotación mundial de MuzHip, sube la Intensity de la luz a 8000, espera 0.06s, y la vuelve a 0.
- AK74M: la rotación del disparo (para hipfire) NO usa RelativeRotation de MuzHip/MuzAim — usa `PlayerCameraManager.GetCameraRotation()` directo (via un Select cuyo bPickA solo es true si el arma actual es la Shotgun — para el AK siempre toma la rotación pura de cámara, sin recoil). Confirma que solo la POSICIÓN de MuzHip/MuzAim importa para la puntería del AK. El desvío "a la izquierda" al apuntar al centro se debía al offset lateral (R=9 a la derecha) que le había puesto al origen — como el proyectil vuela paralelo al eje de la cámara (sin converger a la mira), cualquier offset lateral del origen se nota en distancias cortas. Se sacó el offset lateral (R=0, solo delante y abajo) y se recalculó con la técnica de deproyección con el jugador ya en juego.
- Fogonazo del AK: bajada la Scale del `SpawnEmitterAtLocation` (P_AssaultRifle_MuzzleFlash) a 0.5 para que se vea menos "fuego" y más sutil.
- AK74M sin retroceso visual: el sistema de "camera kick" (`AddShotImpulse` en `MyProject7CameraManager`, C++) solo se llamaba desde 3 ramas viejas hardcodeadas (Strength=2.5 literal cada una, sin ligar a DT_FPWeapons), ninguna alcanzable para el AK. Se agregó una llamada nueva a `AddShotImpulse(Strength=7.5)` en el mismo Sequence del "Fire Event" donde está el fogonazo (GetPlayerCameraManager → Cast a MyProject7CameraManager → AddShotImpulse), bien más fuerte que el resto para que se note con la cadencia rápida del AK (0.1s entre disparos).
- Ninguna arma tenía animación de recarga real (no solo el AK74M): dentro del composite "Reload (R)" de BP_Player, toda la lógica (Set Reloading true → Delay → Set Reloading false, restaurar velocidad, etc., nodo `9EFBAFF24042A594242E5A9994AACFE5`) colgaba del pin "else" de un `Branch` cuyo `Condition` nunca tenía nada conectado (default `true` literal) — la rama `then` (siempre tomada) no iba a ningún lado, así que la recarga real nunca se ejecutaba. Arreglado conectando `Sequence.then_0` directo a ese nodo, saltando el Branch roto. También se detectó que la variable `FPWeaponIndex` (usada para elegir el sonido de recarga por arma) nunca se actualizaba al cambiar de arma (quedaba fija en 3, viejo remanente) — se agregó `Set FPWeaponIndex = Index` al principio de `EquipFP` para mantenerla sincronizada, y se corrigió el chequeo de sonido del AK74M a `FPWeaponIndex==2`.
- Fogonazo con look de "chispas pequeñas" en vez de fuego: reemplazado `P_AssaultRifle_MuzzleFlash` (look de llamarada) por `P_Impact_Metal_Small_01` (efecto de impacto de bala en metal ya incluido en el pack, look de chispas) como `EmitterTemplate` del `SpawnEmitterAtLocation`, con `Scale=(0.3,0.3,0.3)` para que se vea pequeño. También se bajó la `Intensity` de ambas luces de 8000 a 3000 (a esa intensidad, con el color amarillo-naranja y el bloom del post-process, se veía como una bola de fuego en vez de una iluminación sutil). Sigue naciendo desde `MuzHip` (ya calibrado en el eje del cañón), así que la posición no cambió.
- Fogonazo: eliminado el flash de partículas por completo en ambas armas (ya estaba desconectado del AK desde el pedido anterior de sacar las chispas; ahora también se sacó formalmente del flujo compartido) — queda solo la luz amarilla. Se creó `FN502_MuzzleLight` (no existía, solo había luz para el AK) en la posición de `FN502_MuzHip`, mismo patrón que `AK74M_MuzzleLight`. La rama de luz del Fire Event (antes solo encendía la del AK sin condición) ahora está gateada por `FPWeaponIndex` (2→AK74M_MuzzleLight, 3→FN502_MuzzleLight) para que cada arma encienda su propia luz.
- Animación de disparo del AK/FN502 se repetía 2-3 veces por click ("dispara 2 veces más"): el `Sequence Player` del estado "Fire" de ambos AnimBP tenía `bLoopAnimation=true` (default del nodo); como el estado "Fire" se mantiene activo mientras `IsFiring` sigue en `true` (umbral de tiempo, no atado a la duración real de la animación), la animación de disparo (más corta que ese umbral) volvía a arrancar sola varias veces por cada disparo real. Corregido con `set_blueprint_node_property --property-name "Node.bLoopAnimation" --property-value "false"` en el Sequence Player de "Fire" de `ABP_FPS_AK74M` y `ABP_FPS_FN502`.
- Retroceso "desde cero" (código, no animación) para AK y FN502: variables `AK74M_KickOffset`/`FN502_KickOffset` (Vector). En `Fire Event`, gateado por `FPWeaponIndex` (2=AK, 3=FN502), `Set *_KickOffset = (X=-20,Y=0,Z=7)` (fuerte, hacia atrás y arriba) en cada disparo real — no depende de animación ni de `AddShotImpulse`. En el mismo `Event Tick` donde vive el ADS de cada arma: el offset decae cada frame con `VInterpTo(Current=KickOffset, Target=(0,0,0), DeltaTime, InterpSpeed=15)`, y se SUMA (`Add_VectorVector`) al resultado del `VInterpTo` de hip/ADS antes del `SetRelativeLocation` final — así el kick se monta sobre la pose base sin pisar el ADS. Reusa el mismo patrón para ambas armas (mismo Sequence de Tick, misma velocidad de decaimiento).
- Retroceso del AK (y FN502) desincronizado de la cadencia de disparo (el kick de cámara `AddShotImpulse` seguía en curso cuando ya se había disparado 2 veces): `AddShotImpulse` (C++, `MyProject7CameraManager`) solo expone `Strength` por Blueprint, pero el tooltip dice "sincronizado con el disparo" — indicio de que internamente lee la variable de instancia `CurRecoilRecovery` (poblada desde el campo `RecoilRecovery` de `DT_FPWeapons`) para la velocidad de recuperación. El valor que se le había puesto al AK (6) y al FN502 (7) eran casi iguales al del Benelli (5, arma lenta, 0.85s entre disparos) — para una cadencia 8x más rápida (AK: 0.1s) hacía falta un recovery proporcionalmente mayor. Subido a `RecoilRecovery=40` (AK) y `32` (FN502), escalando desde el Benelli por la razón inversa de `FireDelay` (`5 * 0.85/FireDelay_arma`). Sin verificación visual del usuario todavía — si sigue sintiéndose lento, subir más (no hay forma de leer la curva de recuperación real sin acceso al C++).
- ADS del AK74M no hacía nada visible: el nodo que aplica la posición interpolada (`Set Relative Location` sobre `AK74M_Arms`, con su propio `SelectVector`+`VInterpTo` ya armados con los valores ADS/hip correctos) tenía el pin `execute` totalmente desconectado — no venía de mis cambios de esta sesión, ya estaba así (posiblemente de otra sesión trabajando en paralelo sobre el mismo Blueprint: el ADS del FN502 SÍ estaba conectado directo a `Event Tick`, pero cuando se agregó el del AK nunca se insertó un `Sequence` para que corrieran los dos — quedó el nodo del AK "flotando", con sus cálculos armados pero sin disparador). Solución: insertado un `Sequence` entre `Event Tick` y el `Set Relative Location` del FN502 (rama existente, restaurada en `then_0`), con una rama nueva `then_1` hacia el `Set Relative Location` del AK. Método usado para encontrarlo: exportar el EventGraph completo y rastrear la conectividad con un script Python local (BFS sobre `from`/`to` de las conexiones) en vez de perseguir IDs a mano — mucho más confiable para grafos grandes con nodos duplicados/huérfanos.
- Todas las armas (Benelli/AK74M/FN502) disparaban desviadas de la mira: la rotación del disparo usaba `PlayerCameraManager.GetCameraRotation()`, que NO coincide con la rotación real de `FirstPersonCamera` (confirmado en vivo: pitch/yaw/roll distintos incluso con el pawn quieto — el CameraManager del proyecto tiene su propio comportamiento, ajeno a hacia dónde apunta la cámara visual). Sin tocar la cámara ni la posición del arma, se redirigieron las 4 conexiones que usaban ese valor (dentro del composite "Spawn Bullet", anidado en "Fire (LMB)/Fire Event" — sin nombre visible propio; para conectar dentro de un composite anidado sin título, hay que ubicar el nombre del composite CONTENEDOR más cercano con título propio, ej. `--function-name "Spawn Bullet"`, no el del nivel intermedio) a `FirstPersonCamera.K2_GetComponentRotation()` real. Arregla la puntería de las 3 armas a la vez.
- AK74M: aun con la rotación de disparo arreglada, el cañón seguía con ~7.6cm de desvío lateral porque la calibración anterior se había hecho en un instante en que la cámara no miraba perfectamente al frente (el pawn puede heredar rotación residual entre sesiones de PIE). Recalibrado verificando primero `cam.get_forward_vector()` puro `(1,0,0)` antes de tomar la medición, y confirmando después con un line trace desde cámara vs. la posición mundial real del MuzHip (diferencia final ~0.00004, redondeo de float).
- Instalado FN 502 Tactical (paquete fps-animations-fn-502-tactical, FBX único con mesh combinado + 6 animaciones: DefPose/Reload/ReloadFull/Run/Walk/Shot) en la tecla 2: `AssetImportTask`+`FbxImportUI` mínimo (import_mesh/import_as_skeletal/import_animations/mesh_type_to_import=FBXIT_SKELETAL_MESH; las propiedades `import_morph_targets`, `import_bone_tracks` y `animation_length` de `FbxSkeletalMeshImportData`/`FbxAnimSequenceImportData` no existen en la API de Python de UE 5.8, se omiten). El mesh trae 2 material slots ("arms"/"gun") cuyos `MaterialInstanceConstant` auto-generados por el importador quedan con texturas placeholder vacías (DefaultTexture) — hay que crear materiales propios a mano con `build_material_graph` (mismo patrón Substrate `SAMPLERTYPE_Color`/`SAMPLERTYPE_Normal`) y asignarlos como `OverrideMaterials` del componente, no usar los auto-generados. FN502 queda como Index=3 en DT_FPWeapons; tecla 2 = `IA_Weapon2` (ya existía sin mapear) → `EquipFP(3)`. AnimBP: el pin de animación del nodo "Sequence Player" no es un pin normal ni `set_blueprint_node_property` con nombre simple — es la propiedad anidada `Node.Sequence` del struct interno del nodo. `search_nodes` necesita `--graph-name` con el path del state graph para encontrar el node_id de "Sequence Player" (si no, no aparece entre los resultados). Boca de cañón calibrada por deproyección desde el arranque (no a mano) — mismo método que AK74M, sin offset lateral.
- Mapa CQB_Wood_Houses con el interior "mal formado" (≈185 cubos dispersos en 3 zonas inconexas, amasijo solapado + chabolas lejanas): backup en CQB_Wood_Houses_BAK, se conservó el galpón (Floor/Warehouse_*/CeilingLight/CeilingPanel) y se regeneró el interior como laberinto CQB estilo Bodycam con `Scripts/build_cqb_bodycam.py` (BSP por cola sobre rejilla 300, semilla fija, muros OSB M_CQB_Wood_Professional; zona bajo la plataforma a media altura; jaula de entramado, plataforma+rampa+baranda, grada, rincón de sillas+pizarra). Ejecutar con `cfa execute_python --code "g=globals(); exec(open(r'.../Scripts/build_cqb_bodycam.py').read(), g, g)"` (namespace único obligatorio, si no las funciones no ven los globals). Re-tunear editando los parámetros del script y re-ejecutando (borra su propia salida por label CQB_ y respeta el keep-list).

- FN502 ADS: creada transición propia en BP_Player; FN502_Arms interpola normal (-7,-1,-22) a ADS (8,-3.5,-9) con velocidad 24. Compilado/guardado sin errores.

- Regla ADS reutilizable: Aiming + SelectVector + VInterpTo por Tick sobre RelativeLocation del componente brazos+arma. X acerca a cámara, Z sube a la cara, Y centra. Nunca mover la cámara para ajustar el ADS.

- Mapa por defecto: CQB_Wood_Houses configurado para editor, juego y servidor.

- FN502 ADS refinado: target (30,-3.5,-2), conservando normal (-7,-1,-22); compilado sin errores. Pendiente prueba visual del usuario.

- FN502 ADS: se redujo el avance hacia cámara de X=30 a X=14 porque los brazos ocupaban demasiado la vista. Target actual (14,-3.5,-2).

- FN502: corregida inclinación visual con Roll=-15 sobre FN502_Arms; afecta normal y ADS, sin modificar AK.

- FN502 ADS: altura bajada de Z=-2 a Z=-15 porque la pistola quedaba fuera de pantalla. Target actual (14,-3.5,-15).

- FN502: inclinación leve a la derecha con Roll=3; se preservan posiciones ADS/normal.

- Disparo AK/FN502: eliminado por completo el spawn de partículas del fogonazo (SpawnEmitterAtLocation desconectado de la cadena de Fire Event, solo huérfano, no borrado); solo queda la PointLight amarilla puntual (SetIntensity 3000→0) en cada disparo, en ambas armas. No se tocó el sistema de impacto de bala.
- FN502: recarga se reproducía 2 veces al presionar R. Causa: la animación FPS_AnimsArmature_FN_Reload dura 1.98s pero el estado Reload del AnimBP se sostiene 2.75s (Delay del CustomEvent Reload en BP_Player); el Sequence Player tenía Node.bLoopAnimation=true (default) y volvía a arrancar antes de que terminara la ventana. Fix: Node.bLoopAnimation=false en el Sequence Player del estado Reload de ABP_FPS_FN502 (mismo patrón que el bug de Fire). El AK no lo mostraba porque su animación de recarga dura 2.65s, casi igual a los 2.75s.

- Bodycam: cromática visible reforzada en FirstPersonCamera (Fringe 12, inicio 0.58) y r.SceneColorFringeQuality=1 persistente; se conserva el centro legible.

- Láser en punto rojo (las 4 armas, 2026-09-10): agregado componente único `LaserDot` (StaticMeshComponent, esfera pequeña Scale 0.02, material M_LaserDot del pack) a BP_Player, sin depender de qué arma esté equipada. En Event Tick (rama nueva del Sequence del ADS, then_2): LineTraceSingle desde FirstPersonCamera hacia adelante (rango 20000, bTraceComplex=true, bIgnoreSelf=true), y si golpea algo se mueve el LaserDot al ImpactPoint+Normal*0.3 y se hace visible; si no golpea nada, se oculta. No es un haz/línea — es un punto que coincide exactamente con adonde apunta la bala (mismo FirstPersonCamera.K2_GetComponentRotation ya usado para la trayectoria real). Compilado 0 errores, guardado.
\n- Bodycam / PP_BodycamLens: corregida la viñeta que se percibía como suciedad. El material no tenía texturas; se dejó un círculo negro uniforme y visible: EdgeStart=0.56, EdgeEnd=0.82, VignetteIntensity=1.0. Grafo validado sano y asset guardado. Pendiente confirmación visual.\n
- Regresión: ADS de AK y FN502 dejaron de funcionar (2026-09-10). Causa: la conexión entre `Event Tick.then` (vía el nodo `SetScalarParameterValue` del bodycam motion) y el `Sequence` que reparte ADS/retroceso/láser (15756AF1...) se cortó — probablemente por las sesiones de recompilación en caliente durante PIE hechas al probar el láser. Toda la lógica por-frame (ADS de ambas armas, decay del kick de retroceso, y el punto de láser nuevo) quedó sin ejecutarse, sin ningún error de compilación. Fix: reconectar `SetScalarParameterValue.then → Sequence(ADS).execute` con connect_blueprint_nodes. Compilado 0 errores, guardado.
\n- Bodycam: se localizó la causa de la imagen sucia en `FirstPersonCamera.PostProcessSettings`: `FilmGrainIntensity=0.4` con override activo. Se dejó en 0 y se desactivó la viñeta estándar para que no se mezcle; únicamente permanece `PP_BodycamLens` como borde circular negro. BP compilado 0 errores/advertencias y guardado.\n
- Láser activable con la tecla K (2026-09-10): creada `IA_LaserToggle` (Boolean, trigger Pressed) mapeada a K en `IMC_Player`; variable `LaserOn` (bool, default false) en `BP_Player`. Al presionar K se invierte `LaserOn`. En Event Tick, antes de correr el LineTraceSingle del punto de láser se agregó un `Branch(LaserOn)`: si está activo hace el trace y muestra/oculta el punto según impacto; si está apagado, oculta el punto directamente sin tracear. Arranca apagado por defecto. Compilado 0 errores, guardado.
\n- Bodycam revertido a look estable solicitado: viñeta negra fija sin pulso ni movimiento al caminar/correr. En `PP_BodycamLens`, amplitud temporal=0 y multiplicador de BodycamMotion=0; borde conservado con EdgeStart=0.56, EdgeEnd=0.82, intensidad=1.0. Material validado y guardado.\n
- Bodycam pulido (2026-09-10): (1) BUG real — el nodo `CollectionParameter` de `PP_BodycamLens` que lee la velocidad tenía `ParameterName="None"` (y `ParameterId` en ceros); el lado de BP_Player (GetVelocity→VSizeXY→MapRangeClamped 0..650→0..1→SetScalarParameterValue "BodycamMotion") ya estaba bien. Fix: `ParameterName="BodycamMotion"` + `ParameterId` = GUID real del MPC. (2) Movimiento por velocidad reactivado: amplitud (node36 ConstB=0.035) y frecuencia que sube al correr (nodos nuevos 39/40: freq = 1.65 + BodycamMotion*2.5 → node30.B). Quieto = fijo, camina = leve, corre = más visible y rápido. (3) Viñeta más fina y al borde: EdgeStart 0.42→0.70, EdgeEnd 0.60→0.92. (4) Desenfoque leve solo en el borde: 4 SceneTexture taps diagonales (±0.0038 UV, filtered) promediados con el centro, `Lerp(sharp, blur, EdgeMask*0.6)` insertado antes del Multiply final (node27.A ← node59). (5) FirstPersonCamera: `FilmGrainIntensity=0` (override), viñeta estándar `VignetteIntensity=0` (override), `SceneFringeIntensity=1.5` + `ChromaticAberrationStartOffset=0.75` (CA suave solo al borde). (6) `r.Tonemapper.Sharpen=0.5` en DefaultEngine.ini (era 0.75). No hay PostProcessVolume en Backrooms_Level_0 ni CQB_Wood_Houses. La textura "sucia" que se veía en las capturas es el material OSB de las paredes del mapa CQB, no la cámara. Material 0 errores, BP_Player 0/0, guardados.

- Bodycam (corrección importante 2026-09-10): el C++ `AMyProject7CameraManager` con `Bodycam.bPostProcessEnabled=true` FUERZA cada frame la viñeta nativa y la aberración cromática sobre la cámara, pisando lo que se ponga en `BP_Player.FirstPersonCamera.PostProcessSettings`. Los controles REALES son los defaults del BP `/Game/BODYCAM_VFX/BP_Project7BodycamCameraManager` (struct `Bodycam`): `Bodycam.Vignette` 1.0→0.4 (era la causa principal de "cubre demasiado", sobre todo en ultra-wide), `Bodycam.ChromaticAberration` 0.42→0.28, `Bodycam.ChromaticAberrationStartOffset` 0.84→0.82. NO hay knob `Bodycam.FilmGrain`, así que `FilmGrainIntensity=0` en el componente de cámara SÍ vale. `EdgeDarkening=0.05` y `LensEdgeStart/End` (0.90/1.02) son la viñeta de lente del C++, aparte del material. Guardar el BP del manager requiere `compile_blueprint` antes de `save_asset` (si no, "Save failed / read-only").

- Bodycam viñeta v3 (2026-09-10): a pedido del usuario, viñeta al "90% negro" -> node24 VignetteIntensity 1.0→0.9 (el punto más oscuro deja 10% de imagen visible, más suave). Banda aún más fina y al borde: EdgeStart 0.70→0.76, EdgeEnd 0.92→0.97. Nativa `Bodycam.Vignette` 0.4→0.3. CA más concentrada en el extremo: `Bodycam.ChromaticAberration` 0.28→0.25, `Bodycam.ChromaticAberrationStartOffset` 0.82→0.88. Material 0 errores, manager 0/0, guardados. Prueba PIE: centro limpio y nítido, borde apenas perceptible.

- Bodycam viñeta — MECANISMO REAL descubierto (2026-09-10): el C++ AMyProject7CameraManager sobreescribe CADA FRAME los parametros escalares del material PP_BodycamLens desde el struct Bodycam: `Bodycam.LensEdgeStart`->param `EdgeStart`, `Bodycam.LensEdgeEnd`->`EdgeEnd`, `Bodycam.EdgeDarkening`->`VignetteIntensity`, `Bodycam.LensDistortionStrength`->`LensDistortion`. Editar los DefaultValue de los nodos del material NO tiene efecto en juego. Ademas `Bodycam.Vignette` es la viñeta NATIVA de UE (separada, radial suave) — era lo que se veia como "ovalo enorme" en ultra-wide con valor 1.0. Prueba forzada (VignetteIntensity=1.0, EdgeStart=0.10 en los nodos) = CERO cambio visible -> confirmado que el C++ pisa esos params. Valores finales: `Bodycam.EdgeDarkening=0.9` (=90% negro pedido), `Bodycam.LensEdgeStart=0.45`, `Bodycam.LensEdgeEnd=0.70`, `Bodycam.Vignette=0` (solo la del material), `Bodycam.ChromaticAberration=0.25`, `Bodycam.ChromaticAberrationStartOffset=0.88`. Material: radial hecho aspect-independiente (se quito el *aspect en X: node3 crudo -> node10) para que la viñeta se vea igual en 16:9 y ultra-wide; nodos de desenfoque de borde + fix del CollectionParameter de velocidad intactos. Para ajustar: subir/bajar `Bodycam.EdgeDarkening` (oscuridad), `Bodycam.LensEdgeStart` (cuanto entra hacia el centro: mas alto = mas fino). r.Tonemapper.Sharpen=0.5.
\n## Menú principal — 2026-09-09T22:45:24.928951-04:00\n- Aprobado por el usuario: menú `CROSSFIRE ZERO` nativo de UMG al iniciar.\n- Mapa de arranque: `/Game/Maps/MainMenu`; GameMode específico: `/Game/UI/GM_MainMenu`; controlador: `/Game/UI/BP_MainMenuController`.\n- Botones: Madera → `CQB_Wood_Houses`, Backrooms → `Backrooms_Level_0`, prueba → `Weapon_Test`.\n\n## Portada táctica — 2026-09-09T22:48:39.954409-04:00\n- `WBP_MainMenuHome` reproduce la referencia: portada CROSSFIRE ZERO, botones JUGAR/AJUSTES y layout táctico oscuro.\n- Flujo: JUGAR → `WBP_MapSelect`; AJUSTES → `UI_Settings`.\n
- Bodycam viñeta — RESUELTO leyendo el C++ (2026-09-10). La cámara real es `BP_Player>FirstPersonCamera` (UCameraComponent); no hay CameraActor ni PostProcessVolume. El post-proceso lo fuerza cada frame `AMyProject7CameraManager::ApplyBodycam()` (MyProject7CameraManager.cpp:456-598). Mapeo real knob->PP: `Bodycam.Vignette` -> viñeta nativa `max(v,0.05)*0.55` (466) Y param `VignetteIntensity` del material `max(v,0.05)` (545) [piso 0.05: poner 0 no la quita]; `Bodycam.LensEdgeStart/LensEdgeEnd` -> params `EdgeStart`/`EdgeEnd` del material (543-544); `Bodycam.ChromaticAberration`+`ChromaticAberrationStartOffset` (473-478); `Bodycam.FilmGrain` -> `PP.FilmGrainIntensity` (494-495) [el override del componente es inútil]; `Bodycam.LensDirtIntensity` -> `BloomDirtMaskIntensity` con textura de polvo (482-485); `Bodycam.EdgeDarkening` = KNOB MUERTO (no se referencia). Causas de "no cambia nada": (1) `Bodycam.Vignette=0` -> subido a 0.05 por el C++ -> viñeta al 5%; (2) el radial del material se había roto quitando el `*aspect` sobre X (node10 quedó X*X en vez de (X*aspect)^2) -> `r` no pasaba de 0.707 y los `LensEdge*` nunca saturaban. FIX: reconectar node9->node10 A/B (radial aspect-correcto, `r` 0..~1.03 en 16:9 como asume el C++). DESACOPLE (pedido del usuario): renombrado el ScalarParameter del material `VignetteIntensity`->`VigStrength` -> el `SetScalarParameterValue("VignetteIntensity")` del C++ queda no-op y manda el `DefaultValue` del nodo (0.9 = borde 90% negro fijo, solo en el extremo). Knobs finales en `BP_Project7BodycamCameraManager`: `Bodycam.Vignette=0.2` (solo la nativa suave, 0.11), `LensEdgeStart=0.82`, `LensEdgeEnd=1.05`, `ChromaticAberration=0.15`, `ChromaticAberrationStartOffset=0.80`, `FilmGrain=0`, `LensDirtIntensity=0`, `EdgeDarkening=0`. Material node defaults alineados (EdgeStart 0.82 / EdgeEnd 1.05 / VigStrength 0.9 / LensDistortion 0). `r.Tonemapper.Sharpen=0.5` en DefaultEngine.ini [SystemSettings] (sin equivalente cfa). Material 0 errores, BP 0/0, guardados. PIE del proyecto muy inestable: no se pudo capturar prueba visual final, pendiente confirmación del usuario. Para subir/bajar el borde: `Bodycam.LensEdgeStart` (más alto = más fino) y el node24 `DefaultValue` del material (oscuridad, 0-1). Para la viñeta nativa suave: `Bodycam.Vignette`.

- Bodycam viñeta v6 (2026-09-10, pedido "mucho más negra + CA en el borde + sharpen 50%"): material node `VigStrength` DefaultValue 0.9→1.0 (borde 100% negro); `Bodycam.LensEdgeStart` 0.82→0.76 y `LensEdgeEnd`→1.02 (banda algo más ancha); `Bodycam.ChromaticAberration` 0.15→0.45 con `ChromaticAberrationStartOffset`→0.78 (CA marcada pegada a la banda de viñeta, centro limpio). `r.Tonemapper.Sharpen` ya estaba en 0.5 (=50%) en DefaultEngine.ini. Material 0 errores, BP 0/0, guardados. PIE del proyecto no pinta la ventana (arranca en MainMenu), sin captura; pendiente confirmación visual del usuario.
\n- 2026-09-09T22:55:41.370024-04:00: Portada: fondo negro puro y botones rojos con texto blanco.\n\n## Menú: entrada y ajustes — 2026-09-09T22:58:03.098702-04:00\n- Mapa seleccionado: Input Mode Game Only + cursor oculto antes de OpenLevel.\n- Ajustes: resolución ejecuta `r.SetRes`; calidad Baja/Media/Alta y FPS conservados.\n
- Bodycam v7 (2026-09-10): `Bodycam.ChromaticAberration` 0.45→0.9 (CA marcada en el borde; clamp del C++ a 1.5, ".h" avisa que >1.5 pinta arcoíris). `r.Tonemapper.Sharpen` 0.5→0.75 en DefaultEngine.ini [SystemSettings] + aplicado en vivo. BP 0/0 guardado.

- CQB_Wood_Houses regenerado (2026-09-10, pedido "verse como la foto, techo más alto, casas de 2 piso"): reescrito `Scripts/build_cqb_bodycam.py`. Cambios: techo de madera bajo (EAVE 480, ridge ~2600) reemplazado por TECHO ALTO de cerchas de acero oscuro (`M_Metal` de Scene_Warehouse) EAVE 815 / RIDGE 965 / pitch 2° con cordón inferior, montante, diagonales en W y correas; suelo pasado a `M_CQB_DirtyConcrete` (gris); muros de laberinto bajados a 2.70 m (OSB estilo foto) con vanos de puerta + dintel; 4 CASAS DE 2 PISOS bien configuradas (`CQB_L2_*`): planta baja con tabique+vano, losa a z=265, planta alta con muros+vano+baranda 3 lados, ESCALERA RECTA exterior de peldaños (`stair_run`), y PASARELA elevada (`CQB_Catwalk_*`) uniendo 2 casas; plataforma con escalera real en vez de rampa; prop IBC tote añadido. Ejecutado: 220 borrados, 536 spawn, 0 errores. Nivel guardado. Geometría de cajas primitivas: NO queda idéntica a la foto de referencia (render de pack modular); para eso haría falta importar un pack tipo "CQB Modular Core". Re-ejecutar el script re-tunea (borra CQB_ salvo Warehouse/Floor/CeilingLight/CeilingPanel).

## Selector de mapas táctico — 2026-09-09T23:31:51.884776-04:00
- Reemplazado `WBP_MapSelect` por tarjetas panorámicas de CQB Wood Houses, Backrooms Level 0 y Weapon Test.
- Fondo negro `DrawAs=Box`; cabecera, subtítulo y Volver siguen la referencia táctica.
- Los botones conservan el traspaso a `GameOnly` antes de `OpenLevel`; `Volver` retorna a `WBP_MainMenuHome`.
- Validación: 0 errores y 0 advertencias.

- CQB_Wood_Houses (2026-09-10 cont.): (1) Bank Vault (`/Game/Fab/Bank_Vault/scene/StaticMeshes/scene`, escala 0.005, mesh gigante y con pivote descentrado) + 12 Dirty Plastic Drum (`/Game/Fab/Megascans/3D/Dirty_Plastic_Drum_uiujbcxva/.../uiujbcxva_tier_1`) en 4 clusters — se habían spawneado por error en MainMenu (editor recargó ese mapa tras un corte del bridge); se borraron de MainMenu (guardado limpio) y se colocaron en CQB_Wood_Houses. (2) Techo levantado: galpón (Warehouse_Wall/Column/Roof/Beam + CeilingPanel) escalado/subido a ~20 m; techo de acero reconstruido a EAVE 1650 / RIDGE 1900 (script `Scripts/_raise_cqb_roof.py`, 97 actores); luces subidas a z~1510. Nivel guardado. Grafiti pedido: el proyecto NO tiene assets de grafiti/decals con arte — hace falta importar un pack de decals de Fab para poder colocarlos.

- CQB_Wood_Houses (2026-09-10 cont.): (a) muros del galpon revertidos a `M_CQB_BackroomsPaleConcrete` (el script de acero los habia puesto en M_Metal); piso tambien a `M_CQB_BackroomsPaleConcrete` a pedido. (b) `_thick_pipes_cqb.py`: montantes/diagonales/correas de cerchas engrosados a 0.35 (201 actores) + 40 COLUMNAS macizas de acero `CQB_Column_*` (75 cm, M_Metal) de piso a viga (z 2 -> 1960) en grilla que evita los pasillos + perimetro, para que nada quede flotando. Sin textura de oxido en el proyecto: se usa M_Metal. Nivel guardado.
- CQB_Wood_Houses (2026-09-10 cont.): techo simplificado con `_clean_ceiling_cqb.py` -> borradas todas las cerchas/montantes/diagonales/correas/vigas cruzadas (211 actores) y dejadas SOLO 20 vigas de acero horizontales paralelas (CQB_CeilBeam_0..19, span completo en X, z=1900, 40cm ancho). Se conservan faldon del techo, luces y las 40 columnas de piso. Nivel guardado.

- Impacto de bala en metal (2026-09-10): creado decal `/Game/BODYCAM_VFX/Decals/M_Impact_MetalHole` (dominio DeferredDecal, translucent) que usa la textura real `/Game/Decals/T_BulletHole_Metal` (2K) para el color + máscara procedural circular con borde desgarrado (Noise) para la opacidad; Metallic 0.25 / Roughness 0.7. `PM_Metal` -> surface_type SurfaceType1 y asignado como phys material de `M_Metal` (toda la estructura de acero del galpón). En `BP_Bullet_AK47` y `BP_Bullet1` (balas de AK74M y FN502), evento overlap: `BreakHitResult.PhysMat == PM_Metal` -> Branch -> SpawnDecalAtLocation(M_Impact_MetalHole, size 12x8x8, life 60) en la rama true; la rama false conserva el decal genérico. Compilado 0/0, guardado. Quedan nodos GetSurfaceType/Equal(Byte) huérfanos del primer intento (inertes). Pendiente prueba visual en PIE.
- Bodycam (2026-09-10): `Bodycam.LensDistortionStrength` 0 -> 0.1 (Barrel Distortion 10%, se probo 50 y se bajo), `Bodycam.ChromaticAberration` 0.9 -> 1.3 (más franja de color en los bordes; el C++ además la multiplica por 1+LensDistortion*0.5). BP del manager compilado 0/0, guardado.
\n## Menú Crossfire Zero — estado activo de botones (2026-09-10T00:25:46-04:00)\n- `WBP_MainMenuHome`: JUGAR/AJUSTES reciben overlay rojo táctico con animación de opacidad de 0.25 s, texto blanco y bracket HUD en L inferior-derecho. Las scanlines permanecen solamente debajo de JUGAR. Los clics esperan la animación antes de navegar; compilación 0/0.\n
- Chispas de impacto en metal (2026-09-10): sistema Niagara `/Game/BODYCAM_VFX/FX/NS_Impact_Sparks_Metal` con 2 emisores. "Sparks": burst instantáneo de 110 partículas (Directional Burst), cono 68°, vel 650, vida 0.45-1.6s, tamaño 1.2-4.5 varíado, color base [9,2.8,0.4] (rojo-naranja caliente) con curva Scale RGBA que enfría a rojo profundo y se apaga, Gravity+Drag(3.2)+Collision, sprites redondos (Alignment Unaligned) con `M_Spark`. "Embers": burst de 24, vida 3.5-7.5s, tamaño 0.5-1.7, cono 130° vel 175 (caen), color [4.5,1,0.12] con curva que baja el brillo gradual hasta 0, Gravity+Drag(5)+Collision (se posan en el piso). Enganchado en `BP_Bullet_AK47` y `BP_Bullet1`: en la rama de impacto-en-metal (PhysMat==PM_Metal), tras el decal, `SpawnSystemAtLocation(NS_Impact_Sparks_Metal, ImpactPoint, MakeRotFromX(ImpactNormal))`. Todo compila 0/0, guardado.

- Bodycam profesional (2026-09-10): en `PP_BodycamLens` se encontró y corrigió un bug real: el `CollectionParameter` que lee `MPC_BodycamMotion.BodycamMotion` tenía `ParameterName="None"` (roto, nunca leía la velocidad real pese a que BP_Player sí la calculaba y publicaba bien). Se reparó fijando `ParameterName` y su `ParameterId` (GUID) para que coincida con el parámetro real de la colección. Viñeta reducida y llevada al borde exterior (EdgeStart=0.70, EdgeEnd=0.92, antes 0.42/0.60 que tapaba demasiado). Oscilación por velocidad reactivada: amplitud leve (multiplicador 0.035~0.06 sobre BodycamMotion) + frecuencia que sube con la velocidad (BaseFreq 1.65 + BodycamMotion*2.5). Agregado desenfoque muy leve solo en la banda del borde: 4 muestras diagonales de SceneColor promediadas con la muestra central, mezcladas (Lerp) según la misma máscara de viñeta a fuerza ~0.6, sin tocar el centro. Aberración cromática de la cámara suavizada (SceneFringeIntensity 12→1.5, ChromaticAberrationStartOffset→0.75, sincronizado cerca del borde de la viñeta). FilmGrainIntensity y VignetteIntensity estándar reforzados a 0 con override activo (únicamente PP_BodycamLens es la viñeta visible). Sharpen global bajado de 0.9 a 0.5 (`r.Tonemapper.Sharpen` en DefaultEngine.ini, pedido explícito del usuario). Confirmado en PIE: corners con viñeta fina y suave, aberración cromática visible solo en objetos cerca del borde (ej. columna en la esquina), centro completamente limpio. Se descartó que hubiera algún PostProcessVolume en CQB_Wood_Houses ni en Backrooms_Level_0 aportando suciedad — la textura "sucia" que se veía en capturas previas del usuario era el material OSB real de las paredes del mapa, no un artefacto de cámara. Material validado 0 errores/0 huérfanos (60 nodos), BP_Player 0 errores/0 advertencias, ambos guardados.

- CORRECCIÓN importante (2026-09-10): se descubrió que `AMyProject7CameraManager` (C++) reescribe `OutVT.POV.PostProcessSettings` TODOS los frames desde los defaults de `/Game/BODYCAM_VFX/BP_Project7BodycamCameraManager` (`Bodycam.*`), pisando cualquier valor puesto directamente en `BP_Player.FirstPersonCamera.PostProcessSettings` (FilmGrain, Vignette, SceneFringe, etc. — mis ediciones ahí fueron inertes en juego real, aunque compilaban sin error). El control real es `Bodycam.FilmGrain` (confirmado y fijado a 0), `Bodycam.Vignette` (0.2, con piso interno de 0.05 en el C++, nunca llega a "0 total"), `Bodycam.ChromaticAberration`/`ChromaticAberrationStartOffset`, `Bodycam.LensEdgeStart`/`LensEdgeEnd` (que a su vez pisan `EdgeStart`/`EdgeEnd` del material cada frame vía un `UMaterialInstanceDynamic`). Otra sesión de Claude Code trabajó en paralelo sobre esto mismo y ya había hecho ajustes razonables (Vignette=0.2, LensEdgeStart≈0.76, LensEdgeEnd≈1.02, ChromaticAberration=1.3, StartOffset≈0.78, renombrado el parámetro `VignetteIntensity`→`VigStrength` del material para desacoplarlo del C++). Se conservó ese trabajo; solo se forzó `Bodycam.FilmGrain=0` explícitamente (no aparecía en el dump de defaults pero sí existe y se pudo setear). BP del manager compilado 0 errores y guardado.
- Decal de impacto (2026-09-10): a pedido ("reemplaza la que tiene por esa nueva"), el decal viejo `M_BulletHole_Black` de la rama `else` de `BP_Bullet_AK47`/`BP_Bullet1` (nodos `8B8B54F2…`/`7E85AB8C…`, pin `DecalMaterial`) se reemplazó por el cráter quemado nuevo `/Game/BODYCAM_VFX/Decals/M_Impact_MetalHole` (DecalSize 12,8,8). Ahora todos los impactos usan el decal nuevo; el `Branch(PhysMat==PM_Metal)` solo sigue gateando las chispas `NS_Impact_Sparks_Metal`. Compila 0/0, guardado.
\n## Cámara — saturación visual (2026-09-10T00:39:09-04:00)\n- `BP_Player.FirstPersonCamera`: ColorSaturation RGB=0.7, A=1.0. Color sobrio sin llegar a blanco y negro; BP validado 0/0 y guardado.\n\n## Cámara — saturación 25% (2026-09-10T00:40:18-04:00)\n- `BP_Player.FirstPersonCamera`: ColorSaturation RGB=0.25, A=1.0. Colores muy apagados pero visibles; BP validado 0/0 y guardado.\n\n## Cámara — saturación 0% (2026-09-10T00:41:17-04:00)\n- `BP_Player.FirstPersonCamera`: ColorSaturation RGB=0.0, A=1.0; imagen monocromática por pedido del usuario. BP validado 0/0 y guardado.\n- Puntería: la bala se iba "un poco a la izquierda" y bailaba respecto a la mira (2026-09-10). Causa: una sesión previa cambió la dirección del disparo a la rotación CRUDA de FirstPersonCamera, pero la mira (centro de pantalla) sigue el POV FINAL del PlayerCameraManager, al que `AMyProject7CameraManager::ApplyBodycam()` le suma micro-sway (incluso quieto), turn-lag, lean y judder. Medido en PIE: la lógica vieja erraba 1-2 cm lateral a 34 m y oscilaba con el sway. Fix en el composite "Spawn Bullet" de BP_Player: LineTrace desde la ubicación del POV del PlayerCameraManager a lo largo de su forward (con GetCameraRotation del manager); el ImpactPoint (o el punto lejano si no hay hit) se usa como Target de los 2 FindLookAtRotation, con Start = la boca de cañón. Así la bala SALE del cañón y va exacta a la mira a cualquier distancia. Medido tras el fix: 0.00 cm de error. No se tocó arma, cámara, ADS, posición ni mapa. BP_Player compila 0/0, guardado.
- Recarga rota + texto DBG en pantalla (2026-09-10). Sintomas: al disparar/recargar salia "DBG_RELOAD_IF_OUT_ENTRA" en pantalla; y al quedarse sin balas, cada vez que se disparaba solo recargaba (bucle). Causa: (1) nodo PrintString de debug en el composite "Reload If Ammo Out" de BP_Player. (2) la funcion self.Reload() (llamada por IA_Reload y por "Reload If Ammo Out") NUNCA rellenaba la municion: el unico nodo que hacia Set Ammo=Magazine vivia en "Delay for Animation", inalcanzable (sin entrada de exec, resto de leftovers del HK416). Asi Ammo/Ammo2 quedaban en 0 y cada disparo re-disparaba "Reload If Ammo Out". Fix: (A) borrado el PrintString, "Reload If Ammo Out" = Inputs->Reload directo. (B) en el spine real de Reload (Set States[Reloading=true] -> Delay 2.75s -> Set Reloading=false) se inserto antes del Set Reloading=false: Set Ammo=Magazine, Set Ammo2=Magazine, y refresco del TextBlock UI_HUD.Ammo. Probado en PIE: Reload() -> Reloading True, a los ~2.75s Ammo=Ammo2=Magazine(30) y Reloading=False. BP_Player compila 0/0, guardado. Nota: Ammo2 (reserva) ahora se rellena a Magazine en cada recarga -> reserva efectivamente ilimitada (ok para mapa de tiro; si se quiere reserva limitada es otra tarea).
- AK74M animación de recarga (2026-09-10): a pedido, el Sequence Player del estado AnimGraph/AK74MSM/Reload de ABP_FPS_AK74M pasó de FpsAnims_AnimRig_AK_Reload (2.65s) a FpsAnims_AnimRig_AK_Reload_var2 (2.82s), con Node.bLoopAnimation=false. Y el Delay de la función Reload de BP_Player (nodo 3BB532CC del grafo "Reload (R)") subió de 2.75s a 2.85s para que la animación var2 entre completa antes de reponer munición / bajar Reloading. ABP validate pass + compila 0/0; ambos guardados.
- AK74M animación de correr (2026-09-10): a pedido, "Shift+W para correr" con FpsAnims_AnimRig_AK_Run. En ABP_FPS_AK74M: nueva var bool IsRunning; en EventGraph (BlueprintUpdateAnimation) se añadió Cast BP_Player -> Get Sprinting -> Set IsRunning (en la cadena tras Set IsFiring). Nuevo estado "Run" en el state machine AnimGraph/AK74MSM con Sequence Player FpsAnims_AnimRig_AK_Run (bLoopAnimation=true). Transiciones: Walk->Run e Idle->Run (IsRunning), Run->Walk (!IsRunning), Run->Idle (!IsMoving), Run->Fire (IsFiring), Run->Reload (IsReloading). Verificado en PIE: pawn.Sprinting=True -> ABP_FPS_AK74M_C.IsRunning=True con IsMoving=True. Compila 0/0, validate pass, guardado.
- Escopeta Benelli (tecla 3, EquipFP index 1) sin textura (2026-09-10). Causa: el mesh /Game/Anims/FPS_Benelli/Raw/HandWithGloves_AnimateRDY tenia sus 5 slots asignados a los materiales auto-generados del importador FBX (sleeve_st6_generalist, glove_hardknuckle, 12gauge, TTI_Benelli_M4, matchsaverz -> placeholders grises). El pack YA traia 5 materiales propios hechos a mano (M_BenelliArms/Body/Glove/Match/Shell) sin asignar; M_BenelliArms y M_BenelliGlove ademas fallaban ("Missing input texture") por un TextureSample vacio (sampler MASKS) para un mapa packed que el pack no incluye. Fix: borrado ese TextureSample en ambos, Constant 0.75/0.80 -> Roughness, recompilar (0 err). Asignados los 5 M_Benelli* a los slots del mesh por nombre. Verificado en PIE (EquipFP(1), Benelli_Arms visible con textura). Todo guardado.
- SCAR pack (scar (2).zip, tecla 4 pedida) — NO importable por CLI/Python (2026-09-10). El zip es FBX crudo de Blender (source/SCAR.fbx 4.8MB) + 11 PNG (texturas SCARL_LP..., glove_hardknuckle, sleeve_st6_generalist — MISMAS texturas de brazo/guante que la Benelli). El import automatico (AssetImportTask + FbxImportUI skeletal+anims) NO falla del todo pero produce basura: 7 AnimSequences (SCARArmature_Hide/Idle/Reload/Run/Shoot/Take/Walk) + el mesh de brazos+arma como "Circle_001_low" pero SKINEADO A 3 HUESOS DE CAMARA (camera1/Camera/camera_end), escala gigante (extent ~2863), y 8 skeletons/meshes basura de los widgets del rig de Blender (shape_circle/sphere/plane/finger/pose...). Las anims quedan atadas al skeleton de 3 huesos de camara -> inservibles. Borrado /Game/Anims/FPS_SCAR completo. Requiere import por el DIALOGO GUI del editor (arrastrar SCAR.fbx al Content Browser) o re-exportar desde Blender solo con el mesh brazos+arma + su armature de deform (sin camara, sin widgets, aplicar transforms, escala 1). FBX dejado a mano en D:\Unreal Projects\MyProject7\Saved\_scar_import\SCAR.fbx. Cuando el mesh este bien importado: reusar M_BenelliArms/M_BenelliGlove para los brazos (mismas texturas), fila nueva en DT_FPWeapons, IA_Weapon4->EquipFP(4), AnimBP nuevo, MuzHip/MuzAim, etc.
- Pistola T7 (animated-pistol.zip, pedida en tecla 4) — 2026-09-10, EN PROGRESO. A diferencia del SCAR, SI se importo bien: source/T7_Rig.fbx re-importado a import_uniform_scale 0.03 -> mesh desirefx_me_018 con rig FPS real de 125 huesos estilo CoD (j_shoulder_ri/j_elbow_ri/j_wrist_ri/dedos, tag_view en origen), bounds ~45cm. 6 anims T7_RigRig_Joints_{Idle,Hide,Take,Shoot,Reload 3.93s,Watch} en Rig_Joints_Skeleton (NO trae Walk ni Run). Borrado el mesh junk Crosshair. HECHO: (1) M_T7Pistol_Body desde DefaultMaterial_* (samplers de Metallic/Roughness/AO a MASKS, texturas srgb=false TC_MASKS) -> 0 errores. (2) slots de brazo del mesh -> M_BenelliArms / M_BenelliGlove (para igualar al resto de armas, como pidio; OJO: los UV de este mesh son de "ghost classic arms", distintos de sleeve_st6_generalist -> puede estirar la textura, verificar en juego). (3) ABP_FPS_T7Pistol nuevo (skeleton Rig_Joints_Skeleton): state machine T7SM Idle/Fire(Shoot)/Reload con transiciones por IsFiring/IsReloading; EventGraph BlueprintUpdateAnimation castea a BP_Player y setea IsFiring (GameTime-LastFireTime<0.35) e IsReloading (=Reloading). Compila 0/0, validate pass, guardado. FALTA (integracion en BP_Player, ~mitad del trabajo): componente T7Pistol_Arms + T7Pistol_MuzHip/MuzAim + T7Pistol_MuzzleLight; fila T7Pistol en DT_FPWeapons (idx 4, tras Benelli/AK74M/FN502); IA_Weapon4 + mapeo tecla 4 en IMC_Player; IA_Weapon4->EquipFP(4) + rama de equipado/visibilidad para index 4 en BP_Player; calibracion de boca de canon (deproyeccion), ADS, fogonazo en Fire Event gateado por FPWeaponIndex==4.
- Pistola T7 en tecla 4 (2026-09-10, integracion minima "solo aver como se ve", sin fogonazo): en BP_Player -> componente T7Pistol_Arms (SkeletalMesh desirefx_me_018, AnimClass ABP_FPS_T7Pistol_C, hijo de FirstPersonCamera, bHiddenInGame=true por defecto) + T7Pistol_MuzHip/MuzAim (SceneComponent, hijos, SIN calibrar). Fila T7Pistol en DT_FPWeapons (idx 4: BulletClass BP_Bullet1, MagSize 12, FireDelay 0.15, FireSound del FN502). IMC_Player: tecla Four -> IA_Weapon4. EventGraph: IA_Weapon4.Triggered -> EquipFP(4). EquipFP body: insertado SetHiddenInGame(T7Pistol_Arms, NewHidden = NotEqual(Index,4)) tras el hide del FN502; y Branch(Index==4) en el else del Branch(idx==3) -> Set MuzHip/MuzAim = T7Pistol_MuzHip/MuzAim -> LoadFPWeaponStats. OJO: NotEqual_IntInt/EqualEqual_IntInt spawneados por build_blueprint_graph NO tomaron el pin_default B -> hubo que set_pin_default B=4 a mano. Rotacion de brazos: arrancó en Yaw -90 (brazos "muy a la izquierda", j_wrist_ri en right=-37); corregido a Yaw 0 -> j_wrist_ri (f,r,u)=(35,10,-15) = mano derecha natural. Falta afinar posicion fina/ADS/boca de canon y confirmar visualmente jugando (no se puede orientar la camara por script). Event Tick->ADS intacto. BP_Player compila 0/0, guardado.
- Bajar saturacion de la camara (2026-09-10): el C++ AMyProject7CameraManager::ApplyBodycam (lineas 496-500) fuerza PP.ColorSaturation = max(Bodycam.Saturation, 1.3) cada frame, CLAMPeado a un minimo de 1.3 (+30% color). No hay forma de bajarlo por knob ni por el componente FirstPersonCamera (lo pisa el C++), y editar el C++ esta bloqueado. Solucion sin tocar C++: el material post-process PP_BodycamLens (que ya esta en los blendables y corre despues del tonemapper) ahora pasa su salida final (node 27) por un nodo Desaturation con Fraction = nuevo ScalarParameter DesatAmount (default 0.30) antes del EmissiveColor. Eso quita ~30% de saturacion despues del +30% del C++ -> queda cerca de neutro. Subir DesatAmount para menos color, bajar para mas. Compila 0 err, guardado.
- Guantes verde militar para la pistola T7 (tecla 4) (2026-09-10): materiales nuevos M_T7Pistol_Gloves y M_T7Pistol_Arms desde las texturas propias del pack (vm_ghost_classic_gloves_1_1 / vm_ghost_classic_arms_1_2, UV correcto -> sin estiramiento) x Constant3(0.34,0.44,0.20) (verde oliva) en BaseColor, Normal propio, Roughness 0.75. Asignados a los slots del mesh desirefx_me_018: arms/base_male_arm_l/buckle -> M_T7Pistol_Arms, gloves -> M_T7Pistol_Gloves. Verificado en PIE: brazos verdes con detalle de guante. 0 errores, guardado.
- Pistola T7 (tecla 4): cámara más cerca + ADS como la FN502 (2026-09-10). "Más cerca": T7Pistol_Arms RelativeLocation de (0,0,0) a (-9,0,3) (con Yaw 0, -X acerca hacia la cámara, +Z sube). ADS: cadena IDÉNTICA a la del FN502 insertada en Event Tick entre SetScalarParameterValue.then y el Sequence de ADS -> Set Relative Location(T7Pistol_Arms, VInterpTo(Current=GetRelLoc, Target=SelectVector(A=ADS, B=hip, bPickA=Get Aiming), InterpSpeed=24) + Get T7Pistol_KickOffset). Nueva var T7Pistol_KictOffset (Vector, 0). Valores: hip=(-9,0,3), ADS=(-13,-8,13) -> en ADS la muñeca queda a (f23.7, r2.0, u-2.0) = centrada, a la altura de los ojos. Para afinar: pines A (ADS) / B (hip) del SelectVector E4A08CE6, o el RelativeLocation del componente para el hip. BP_Player compila 0/0, Event Tick->resto de cadenas ADS intacto, guardado. (Nota: en PIE headless Aiming sale True fijo, es artefacto del test; la FN502 usa la misma var y va bien en juego.)
- T7 ADS ajuste (2026-09-10): a pedido "más atrás y más abajo" -> SelectVector E4A08CE6 pin A (pos ADS) de (-13,-8,13) a (2,-8,2). Con Yaw 0: +X aleja de la cámara (más atrás), Z más bajo = más abajo. Estimado: muñeca a ~(f38, r2, u-13). Pendiente confirmación visual del usuario apuntando en juego (no se puede forzar Aiming por script).
- T7 (tecla 4) — guantes iguales al AK (2026-09-10): a pedido "mismo color/largo/grueso que el AK". El material 'arms' del AK (/Game/Anims/FPS_AK74M/Raw/arms, textura T_arms_Color 4K, sin tint) se asigno directamente a los 4 slots de brazo del mesh desirefx_me_018 del T7 (arms/gloves/base_male_arm_l/buckle). MISMO material, misma textura, mismo verde EXACTO que el AK. El "largo y grueso" (geometria del brazo) NO se puede igualar por cfa: el T7 usa el mesh desirefx_me_018 (esqueleto propio, estilo CoD) y el AK usa FpsAnims — son mallas distintas; igualar la forma necesitaria Blender o cambiar el mesh (rompe las anims del T7). Materiales verdes propios anteriores (M_T7Pistol_Gloves/Arms) quedan sin usar. slot 4 (pistola) sigue con M_T7Pistol_Body. Guardado.
- T7 (tecla 4) — geometria de brazo sobrante "abajo" (2026-09-10): el mesh desirefx_me_018 tiene una seccion extra en el slot base_male_arm_l (un brazo desnudo separado, aparte de sleeves+gloves) que asomaba por abajo. Solucion: material nuevo M_T7Pistol/Materials/M_Invisible (blend Masked, OpacityMask = Constant 0 -> toda la seccion se descarta, no renderiza) asignado al slot base_male_arm_l. Los brazos visibles quedan = slots vm_ghost_classic_arms_1_1 (sleeves) + vm_ghost_classic_gloves_1_1 (gloves), ambos con el material arms del AK. No se puede engrosar la geometria por cfa; si hace falta cubrir mas se puede subir RelativeScale3D del componente T7Pistol_Arms. PENDIENTE confirmar visualmente (no se pudo orientar la camara del PIE para captura). Si desaparecieron las manos, revertir el slot y probar ocultar otro.
- T7 (tecla 4) recarga que "no termina bien" (2026-09-10): la anim T7_RigRig_Joints_Reload dura 3.93s pero el estado Reload de ABP_FPS_T7Pistol se sostiene solo 2.85s (Delay compartido 3BB532CC de BP_Player "Reload (R)") -> se cortaba ~1.1s antes de terminar. Fix: Node.PlayRate=1.45 en el Sequence Player del estado AnimGraph/T7SM/Reload (B133C097) -> 3.93/1.45 = 2.71s, entra en la ventana con margen. ABP compila 0/0, guardado. (Mismo patron que el reload doble del FN502; alternativa: hacer el Delay por-arma con un Select por FPWeaponIndex, no hecho para no tocar AK/FN502/Benelli.)
- T7 (tecla 4) - eliminar guantes/geometria sobrante: los slots vm_ghost_classic_gloves_1_1, base_male_arm_l y buckle_master_urban del mesh desirefx_me_018 -> M_Invisible (Masked, OpacityMask=0). Queda visible solo vm_ghost_classic_arms_1_1 (slot 0, material 'arms' del AK) + la pistola (M_T7Pistol_Body). Si slot 0 no incluye manos habria pistola flotante -> revertir y dejar gloves visible.
- Carpeta del arma de la tecla 4: /Game/Anims/FPS_T7Pistol/ . Anims en /Raw/: T7_RigRig_Joints_Idle (0.03s), _Hide (0.5s), _Take (0.67s), _Shoot (0.5s), _Reload (3.93s), _Watch (6.67s). Skeleton Rig_Joints_Skeleton. NO trae Walk ni Run.
- AK-74M (tecla 1) - disparo realista desde el cañón (2026-09-10):
  1. Eliminado el "disparo artificial": los 5 nodos huerfanos de Fire Event que reusaban P_Impact_Metal_Small_01 (scale 0.3) como fogonazo falso (SpawnEmitterAtLocation 589D749C + Get MuzHip B11A5D32/49A70112 + Get World Location 4B43A570 + Get World Rotation 79456E95). Ya estaban desconectados; ahora borrados. dangling_exec_chains 39->38.
  2. Bala nace en el cañón: AK74M_MuzHip y AK74M_MuzAim RelativeLocation de (0.1346,65.0076,24.0) -> (0,50,13). Antes el punto flotaba ~12cm POR ENCIMA del cañón (deproyeccion al eje de camara); ahora esta en la linea del cañón (medido: 1.2cm del hueso Rif, ~19cm delante = boca real). Bones del AK: Rif/Rif_end = hueso del rifle, PBody, Trigger, Safety, Pmag/Pmag2 (esqueleto de 85 huesos, nombres Arm_L/UpArm_L/Forearm_L/Hand_L/Bone_L_NNN...).
  3. Fogonazo realista: SpawnEmitterAttached(P_AssaultRifle_MuzzleFlash de MilitaryWeapDark, AttachToComponent=AK74M_MuzHip, Scale 0.4) insertado en la rama AK de Fire Event (Branch FPWeaponIndex==2 [171E096D].then -> flash -> SetIntensity(MuzzleLight 3000)). Sigue al cañón porque va attached.
  4. "Va donde apunto": ya lo hace el composite "Spawn Bullet" (LineTrace del POV del PlayerCameraManager al punto de impacto real -> FindLookAtRotation(bocacanon -> ese punto)); no se toco. Ahora la bala nace en el cañón Y converge en la mira.
  BP_Player compila 0/0, guardado.
\n## Diagnóstico AK74M — boca de cañón visual (2026-09-10T12:12:49-04:00)\n- La trayectoria usa cámara→impacto y ProjectileOffset=(0,0,0). La bala nace solo de `AK74M_MuzHip/MuzAim`, ambos en (0,50,13). El offset visual de ~5 cm proviene de la calibración del muzzle respecto al modelo; no se alteró sin autorización.\n\n## AK74M — recalibración de boca de cañón (2026-09-10T12:15:59-04:00)\n- Solo `AK74M_MuzHip` y `AK74M_MuzAim`: (0,50,13) → (-5,50,13), 5 cm hacia la derecha visual. Trayectoria, arma, ADS y cámara intactos. BP 0/0 y guardado; pendiente prueba visual.\n
## AK74M — muzzle centrado en el cañón (2026-09-10)
- El disparo/tracer salía a un lado del cañón: `AK74M_MuzHip` y `AK74M_MuzAim` tenían X=-5 (offset lateral a mano). El eje del arma en el modelo es X=0 (malla simétrica en X, hueso `Rif` y `PBody` en X~0, medido con actor temporal). Corregido X -5 -> 0 en ambos; Y=50, Z=13 (Z coincide con `Rif`). Trayectoria intacta (usa cámara->impacto + FindLookAtRotation desde el muzzle). BP_Player 0/0 y guardado.

## Menu principal — el operador no se veia (2026-09-10)
- El personaje `MM_Operator` (SkeletalMeshActor, mesh `/Game/UI/Operator/Mark_1_Olive_IdlePose`, anim `ABP_MenuOperator`) ya estaba colocado en `MainMenu.umap` junto con una `CineCameraActor` `MM_MenuCam`, pero `BP_MainMenuController` (BeginPlay) nunca fijaba view target -> la vista usaba el SpectatorPawn en `PlayerStart` (0,0,92) mirando +X y el operador (-38,-128,0) quedaba fuera de cuadro. Fix: en el EventGraph de `BP_MainMenuController` insertado antes de Create Widget: `Get Actor Of Class`(CineCameraActor) -> `Set View Target with Blend`(Target = Get Player Controller, BlendTime 0). Ademas `MM_MenuCam` re-apuntada a rot (pitch -2.5, yaw -165, roll 0) para encuadrar al operador a la derecha (texto del menu a la izquierda). Verificado en PIE: personaje visible. BP 0/0 y guardado; MainMenu.umap guardado.

## Menu principal — el fondo tapaba al operador (2026-09-10)
- Tras poner el view target, el personaje seguia sin verse: `WBP_MainMenuHome` tiene un Border `Bg_Black` a pantalla completa con `BrushColor` negro opaco (A=1) que tapaba el 3D. Bajado a `A=0.4` (scrim semitransparente: se ve el operador detras y el texto conserva contraste). Widget compila 0/0 y guardado. Si se quiere mas/menos oscuro, ajustar `Bg_Black.BrushColor.A`.

## Menu principal — acercar camara y quitar bordes de la sala (2026-09-10)
- `MM_MenuCam` reubicada a loc (352,-315,131) rot (pitch -3.4, yaw 148.5, roll 0), focal 29mm. Encuadre: operador cuerpo entero en 3/4 de perfil, tercio derecho del cuadro, pared clara lisa detras; se pierden de vista el borde superior de la pared, el rincon oscuro y el hueco negro de la derecha que se veian antes. Mitad izquierda libre y oscura para el texto del menu. Geometria de la sala (cajas): suelo z-20, techo z500, pared O x=-820, pared N y=760, pared S y=-640. MainMenu.umap guardado.

## Menu principal — camara: solo pared de fondo, sin bordes (2026-09-10)
- El "borde negro" no era geometria sino la caida de luz en `MM_WallBack` (pared unica X=-820, Y span -1660..1740): el charco iluminado por los spots es una seccion, el resto de la pared queda en sombra y se ve negro. Solucion: encuadre mas cerrado y casi perpendicular a la pared para que el cono de 44.5 grados (focal 29mm, sensor 23.76) caiga entero dentro del charco iluminado. `MM_MenuCam` -> loc (280,-195,128), rot (pitch -4, yaw 168.1). Operador centrado, cuerpo entero, fondo = pared lisa iluminada. MainMenu.umap guardado. NOTA: el editor se quedo pillado varias veces al ciclar PIE (bug conocido del proyecto); verificar dando a Play, no por screenshot de PIE.

## Bodycam — viñeta más fina y garantizada en negro puro + CA más notoria (2026-09-10)
- `Bodycam.LensEdgeStart` 0.90→0.85, `LensEdgeEnd` 1.03→0.99 (la máscara llega a 1.0 antes del borde real de pantalla, así el negro es 100% puro en la esquina en vez de quedarse en gris por no alcanzar el tope). `ChromaticAberrationStartOffset` 0.78→0.65 y `ChromaticAberration` 1.3→1.6 para que se note claramente por encima de la viñeta. `VigStrength` del material ya estaba en 1.0 (sin tinte de color posible, el material no tiene parámetro de color — el multiply va directo a negro). BP 0/0, guardado.

## Bodycam — fisheye/barrel confirmado dentro del rango pedido (2026-09-10)
- Pedido "Fisheye/Barrel Distortion 7-10% vía post-process material": ya existía como `Bodycam.LensDistortionStrength` (post-process material `PP_BodycamLens`, param `LensDistortion`, ensancha FOV y sube aberración en bordes). Estaba en 0.10 (10%, tope del rango); dejado en 0.08 (8%) y luego subido a 0.18 (18%, a pedido "que se note aún más" - por encima del rango original 7-10%). `bLensEnabled=True`, `bPostProcessEnabled=True`. BP 0/0, guardado.

## Balas — agujero no marcaba a corta distancia + chispas/decal viejo en impactos de pared (2026-09-10)
- **Causa 1 (agujero no marca cerca)**: en `BeginPlay` de cada bala, `ColSphere` (esfera de overlap para daño a Pawn) se ponía `NoCollision` -> `Delay Until Next Tick` -> `QueryAndPhysics`. Esa espera de 1 tick, a la velocidad de la bala, equivale a recorrer 1-2 metros con la colisión de overlap apagada — si el objetivo estaba dentro de esa distancia, nunca se detectaba. Fix: se quitó el `NoCollision`+`Delay` y en su lugar se llama una sola vez `ColSphere -> Ignore Actor When Moving(GetOwner(), true)` antes de activar `QueryAndPhysics` de inmediato (sin espera) — el propio disparador queda ignorado, así que no hay auto-impacto y ya no depende de la distancia. Aplicado en `BP_Bullet_AK47`, `BP_Bullet1`, `BP_Bullet_Shotgun` (BP_Bullet_Scar ya no tenía este bug).
- **Causa 2 (chispas/decal nuevo no se ven en la mayoría de impactos)**: el sistema de chispas (`NS_Impact_Sparks_Metal`) y el decal nuevo (`M_Impact_MetalHole`) solo estaban conectados en la rama de `OnComponentBeginOverlap(ColSphere)` (impacto contra Pawns). El evento `Event Hit` (colisión física real, el que dispara al pegarle a paredes/estructuras — el caso más común) seguía con un `SpawnDecalAtLocation` suelto sin chequeo de material y sin chispas ("la vieja"). Fix: en `Event Hit` de las 4 balas activas (`BP_Bullet_AK47`, `BP_Bullet1`, `BP_Bullet_Shotgun`, `BP_Bullet_Scar`) se insertó `BreakHitResult(Hit) -> PhysMat == PM_Metal -> Branch`: si es metal, `SpawnSystemAtLocation(NS_Impact_Sparks_Metal, HitLocation, MakeRotFromX(HitNormal))` antes del decal; en ambos casos el decal existente ahora usa `M_Impact_MetalHole` (antes indefinido/viejo) con `LifeSpan=60`.
- Armas cubiertas (las 4 filas activas de `DT_FPWeapons`): AK74M->BP_Bullet_AK47, FN502 y T7Pistol->BP_Bullet1, Benelli->BP_Bullet_Shotgun. `BP_Bullet_Scar` corregido también (sin fila activa en la DataTable ahora mismo). `BP_Bullet` base no se tocó (sin dependents, no lo usa ningún arma).
- Los 4 blueprints compilan 0/0, guardado. Pendiente prueba visual en PIE (disparar de muy cerca y contra distintas superficies).

## Bodycam — distorsión de lente rota a 18% (mancha negra lateral) (2026-09-10)
- Al subir `Bodycam.LensDistortionStrength` a 0.18 para que "se note más", el shader de barril empezó a muestrear fuera del render (UV fuera de rango) -> mancha negra grande y asimétrica en un lado de la pantalla (visible en PIE, capturado por el usuario). Bajado a 0.09 (dentro del 7-10% original, límite seguro conocido). BP 0/0, guardado.

## Bodycam — viñeta "gruesa" era la ventana angosta, no los números (2026-09-10)
- El usuario veía una mancha negra enorme (medio pantalla) con `LensEdgeStart=0.85/LensEdgeEnd=0.99`, que en ventana ancha normal (probado en PIE a 1907px, sin mancha) se ve bien fina. Se sospechó bug de aspect ratio en `PP_BodycamLens` (revisado el grafo completo: `ViewProperty(MEVP_VIEW_SIZE)` -> ComponentMask R/G -> Divide(Width/Height) está bien ordenado, no invertido). El problema apareció al usar una ventana de juego angosta/vertical; tras redimensionar/reiniciar la ventana de preview quedó bien ("ahí está bien", confirmado por el usuario).
- Estado final confirmado bueno: `Bodycam.LensEdgeStart=0.85`, `LensEdgeEnd=0.99`, `ChromaticAberration=1.6`, `ChromaticAberrationStartOffset=0.65`, `LensDistortionStrength=0.0` (fisheye desactivado por ahora, pendiente reintroducir con cuidado en valor bajo tipo 0.03-0.05 si se pide). `VigStrength`=1.0 (negro puro, sin parámetro de color en el material).
- Pendiente si vuelve a pasar: probar explícitamente con ventana angosta/vertical vía `r.setres AxBw` en una ventana PIE separada (`play_in_editor --new-window`) para reproducir y depurar el aspect ratio real, en vez de asumir que es el material.

## Bodycam — causa real de la mancha negra: multiplicación de aspecto rota en el radio de la viñeta (2026-09-10)
- Diagnóstico definitivo: en el grafo de `PP_BodycamLens`, el radio de la viñeta se calculaba como `sqrt((X_centrado * aspecto_ViewSize)^2 + Y_centrado^2)` (nodo Multiply_0 multiplicando X por `ViewSize.Width/ViewSize.Height`). Esa corrección de aspecto es innecesaria y se dispara mal en ventanas angostas/no-16:9: el radio en X se descontrola y la máscara llega a 1.0 (negro total) mucho antes de tiempo, cubriendo medio cuadro — coincide exactamente con lo que reportó el usuario (mancha enorme en un lado, ventana no estándar).
- Fix real: se quitó la multiplicación por aspecto — ahora el radio usa X/Y crudos en espacio UV centrado (`sqrt(X^2+Y^2)`), que por definición da 0.707 en las 4 esquinas y 0.5 en la mitad de cada borde **sin importar la resolución/aspecto de la ventana** (UV siempre es 0-1 por eje). Recalibrado `EdgeStart`/`EdgeEnd` a esta nueva escala: 0.85/0.99 (que asumían la escala rota) -> **0.58/0.74**. Sincronizado en el material (`PP_BodycamLens` scalar param defaults) y en `Bodycam.LensEdgeStart/LensEdgeEnd` del BP (que es lo que de verdad manda en juego, el C++ lo pisa cada frame). `bLensEnabled` reactivado (se había apagado como diagnóstico). Verificado en PIE ventana nueva: sin mancha, CA solo en bordes, esquinas con oscurecido leve normal.
- Si alguna vez se quiere reintroducir distorsión de barril real (`LensDistortion`, ahora en 0.0), ojo: usa el mismo `Add_0` (radio^2) ya corregido, así que ahora también es aspect-independiente — debería ser seguro subirla de a poco (probar 0.03-0.05 primero) sin repetir el bug.

## Balas: bala invisible (menos escopeta), fogonazo eliminado, chispas solo en escopeta y mejoradas (2026-09-10)
- **Bala visible oculta**: `Sphere` (StaticMeshComponent, el mesh de la bala volando) puesto `bVisible=false` en `BP_Bullet_AK47`, `BP_Bullet1` (FN502/T7Pistol), `BP_Bullet_Scar`. `BP_Bullet_Shotgun` intacto (sigue viéndose). La colisión/Event Hit no se toca (bVisible solo afecta render).
- **Fogonazo eliminado del todo**: en `BP_Player`, grafo compuesto `Fire (LMB)` -> `Fire Event`, se borraron las 2 secuencias `Get MuzzleLight -> SetIntensity(brillo) -> Delay -> Get MuzzleLight -> SetIntensity(0)` (una para `AK74M_MuzzleLight`, otra para `FN502_MuzzleLight` — eran las únicas 2 armas con luz de fogonazo). BP_Player 0/0 compilado.
- **Chispas de impacto**: ahora SOLO se spawnean en `BP_Bullet_Shotgun` (a pedido explícito, "que solo se vea en la escopeta no en la AK ni la pistola"). En `BP_Bullet_AK47`, `BP_Bullet1` y `BP_Bullet_Scar` se quitó el nodo `SpawnSystemAtLocation` de la rama Event Hit (el decal de impacto se conserva en ambas ramas Metal/no-Metal, solo se quitó la chispa).
- **NS_Impact_Sparks_Metal mejorado**: color mucho más rojo puro (antes anaranjado [9,2.8,0.4], ahora curva que arranca [14,0.15,0.02] y se apaga a rojo oscuro, mismo tratamiento en el emisor "Embers"). Vida de "Sparks" extendida de 0.45-1.6s a 1.6-2.4s (~2s). Spawn count subido 110->160 (Sparks) y 24->36 (Embers) para que caigan más. **Bug encontrado y arreglado**: el módulo `Collision` (colisión por depth-buffer de la cámara) hacía que algunas partículas se quedaran congeladas en el aire hasta que la cámara "las detectaba" de nuevo y entonces caían de golpe — comportamiento clásico y conocido de la colisión por depth-buffer de Niagara. Desactivado `Collision` en ambos emisores (Sparks y Embers); ahora caen solo por gravedad+drag, sin colisión, sin trabarse. Sistema compila 0/0, guardado.

## Balas: eliminado el efecto de impacto viejo (partículas amarillas/rectangulares) (2026-09-10)
- El "rastro amarillo tipo rectángulo" que se veía en cada disparo no era mío ni las chispas nuevas: era un `SpawnEmitterAtLocation` heredado (particle system viejo estilo Cascade) que ya estaba metido justo antes del decal, en AMBAS ramas de impacto (overlap contra Pawn y Event Hit contra el mundo) de las 4 balas activas. Se borró ese nodo en las 8 cadenas (AK74M, FN502/T7Pistol, Benelli, SCAR x2 ramas c/u salvo SCAR que solo se tocó su Event Hit) y se reconectó `ApplyPointDamage -> (rama de material/decal)` directo, sin el emisor viejo en medio. El decal y (en la escopeta) las chispas nuevas de Niagara siguen intactas. Las 4 balas compilan 0/0, guardado.

## AK74M: quedaba un fogonazo de partículas sin quitar (2026-09-10)
- Al borrar antes la luz de fogonazo, quedó vivo un segundo nodo separado: `SpawnEmitterAttached` (adjunto a `AK74M_MuzHip`) dentro de la rama `FPWeaponIndex==2` de `Fire Event` — un efecto de partículas independiente de la luz, por eso seguía viéndose el fogonazo del AK después del primer fix. Borrado. `Get AK74M_MuzHip` se dejó intacto (lo usa también "Spawn Bullet" para el origen de la bala). BP_Player 0/0, guardado.

## Backrooms: corrección de mapa destino (2026-09-10 noche)
- Usuario reportó BR26_* dentro de MainMenu durante trabajo paralelo. Limpieza exclusivamente de BR26_* en MainMenu; operador/cámara/luces/PP originales intactos. Reconstrucción guardada en `/Game/Maps/Backrooms_Level_0`: 214 mallas y 79 fluorescentes, materiales independientes `/Game/Maps/Materials/M_BR26_{Paper,Carpet,Ceiling,Base,Frame,Lamp}`. No sustituirlos por los materiales de madera CQB.
- Backup `/Game/Maps/Backrooms_Level_0_Backup_20260910`. Puerta hacia madera conservada. Validar ruta del mundo DENTRO de la misma ejecución que modifica geometría y justo antes de guardar; comandos separados permiten que otra sesión cambie el mapa entre ellos.
- Scripts `br_repair.py` y `br_verify.py` en `C:/Users/grego/Documents/Codex/2026-09-08/co`. Estado: aplicado, aprobación visual pendiente; distribución y papel procedural aproximan la referencia, no son réplica exacta.

## CQB reconstruido desde referencias (2026-09-10 noche)
- Via CodeFizz, backup `/Game/Maps/CQB_Wood_Houses_Backup_20260910_Reference`. Nivel original conserva su ruta. Seis casas iguales ampliadas a 12x10m, dos plantas, losa superior z302cm, puertas/ventanas, corredores, barandas, siete puentes y cuatro escaleras. Prefijo `CQB_R26_`. 539 actores anteriores reemplazados, recuperables en backup.
- Materiales propios `M_CQB_R26_PaleOSB`, `M_CQB_R26_Frame`, `M_CQB_R26_Lamp`; compilados sin errores. Concreto conservado. Distribución interpretada de imágenes, no copia exacta de un plano.
- Usuario rechazó iluminación blanca excesiva 7500/550/3000. Ajuste final guardado: 12 luces techo 2800, 12 interiores 180, 3 rellenos 250; temperatura 5800K e indirecta 1. Pendiente aprobación visual y recorrido completo en juego.
- Receta en `C:/Users/grego/Documents/Codex/2026-09-08/co/build_cqb_reference.cjs` y archivos cqb_*. No reejecutar sin limpiar salida propia.

## Encontrado el fuego real: cada bala se lanzaba su propio destello al nacer (2026-09-10)
- El "fuego a la izquierda del cañón" no venía de `BP_Player` (ya estaba limpio ahí) sino de las propias balas: en `BeginPlay` de las 4 (`BP_Bullet_AK47`, `BP_Bullet1`, `BP_Bullet_Shotgun`, `BP_Bullet_Scar`), justo después de `ClientStartCameraShake`, había un `SpawnEmitterAtLocation` heredado (un puff/destello en el punto de spawn de la bala, con distinta escala por arma: 0.52/0.4/0.5/0.5) que nunca se había tocado. Borrado en las 4, reconectado `ClientStartCameraShake -> (lo que seguía)` directo. Las 4 compilan 0/0, guardado.
- **Calibración revisada** (`RelativeLocation` de los `MuzHip`): `AK74M_MuzHip`=(0,50,13), `FN502_MuzHip`=(0.08,62,25) — ambos con valores propios, parecen bien medidos. `Benelli_MuzHip`=(62.5,5.8,48.8) — esquema de ejes distinto (probable socket/padre diferente al de las otras armas, no necesariamente mal). **`T7Pistol_MuzHip` y `T7Pistol_MuzAim` están en (0,0,0)** — sin calibrar, nunca se movieron del origen; la bala del T7 probablemente nace en el punto de anclaje del arma y no en la boca real del cañón. Pendiente medir y corregir.

## Bamboleo de caminar/correr muy sutil, subido (2026-09-10)
- Revisado el código del bob (`MyProject7CameraManager.cpp` ~L200): la fase avanza con `Speed2D * DeltaTime * BobSpeed` (distancia real recorrida, no un timer) — el diseño ya está bien, sincronizado al paso. El problema era solo amplitud baja. `Bodycam.WalkBobAmount` 0.35->0.70, `Bodycam.SprintBobAmount` 1.05->2.0 (doblado, mantiene proporción). BP 0/0, guardado. Si sigue sintiéndose poco, seguir subiendo estos dos valores (afectan verticalidad, balanceo lateral, roll y pitch todos juntos, proporcionalmente).

## Bala visible (trazador) y chispas de impacto en AK/pistola (2026-09-11)
- **Chispas que no salían**: el spawn de `NS_Impact_Sparks_Metal` solo estaba en la cadena `OnComponentBeginOverlap` (que únicamente dispara contra Pawns) y además colgado de la rama `PhysMat == PM_Metal`. Al disparar a paredes/suelo corre la cadena `Event Hit`, que no tenía ningún spawn de chispas. Añadido `SpawnSystemAtLocation(NS_Impact_Sparks_Metal, HitLocation, MakeRotFromX(HitNormal))` entre `ApplyPointDamage` y el `Branch` en `Event Hit` de `BP_Bullet_AK47` y `BP_Bullet1` — **fuera del branch de PM_Metal a propósito**: los mapas son madera/concreto/alfombra, si se deja condicionado al metal no se ve nunca.
- **Bala invisible**: el componente `Sphere` estaba en `bVisible=false` (AK/pistola). Al volverlo visible se veía mal porque era `/Engine/BasicShapes/Sphere` a escala (1,1,1) = **bola de 1 metro** con material `Arealight`. Convertido en trazador: escala alargada en X (dirección de vuelo del ProjectileMovement), `CastShadow=false` y material nuevo `/Game/BODYCAM_VFX/FX/M_BulletTracer` (Unlit, emisivo ámbar 6/2.4/0.5 — no subir mucho más o el bloom parece fogonazo).
- Escalas finales: AK `(0.45, 0.03, 0.03)` = 45cm; pistola/FN502 `(0.30, 0.028, 0.028)` = 30cm; escopeta `(0.05,0.05,0.05)` = perdigón de 5cm (antes era la misma bola de 1m). Velocidad del proyectil 12000 cm/s en las tres, sin gravedad: a 60fps avanza ~2m por frame, así que el trazador se ve como un destello rápido — es lo esperado, si se quisiera más continuo hay que alargar la X, no bajar la velocidad.

## Chispas de impacto: el "cubo grande", forma redonda y rojo vivo (2026-09-11)
- **Causa del cubo**: `M_Spark` era aditivo sin máscara de forma (emisivo = `SparkColor`×`Fade` constantes) → cada partícula se dibujaba como cuadrado sólido, y además ignoraba el color de la partícula (las curvas de color de Niagara no hacían nada). Encima el emisor `Sparks` tenía `Sprite Size Mode` en "Non-Uniform" → usaba el `Sprite Size` por defecto (grande) en vez de su rango 2–14cm, y `ScaleSpriteSizeBySpeed` lo estiraba ×3.3.
- **Ojo con el enum `ENiagara_SizeScaleMode`**: el valor guardado NO sigue el orden visual. `NewEnumerator0`=Unset, `3`=Uniform, `1`=Random Uniform, `4`=Non-Uniform, `2`=Random Non-Uniform. Se pasa como string: `--value '"NewEnumerator2"'`.
- `M_Spark` rehecho (solo lo usa `NS_Impact_Sparks_Metal`): máscara radial `1-saturate(Distance(UV,0.5)*2)` → `^2.2` halo + `^7` núcleo; halo = `ParticleColor.RGB × (1,0.08,0.02)` (tinte rojo dentro del material, así también se ve rojo en la vista previa del editor, donde ParticleColor es blanco); núcleo naranja tenue `(1,0.45,0.05) × ParticleColor.G × 0.35`; todo × `ParticleColor.A`.
- Niagara: color inicial de ambos emisores a blanco (antes 9/1.7/1.1 × curva = saturaba a blanco), curvas `Scale RGBA` rojo con verde/azul casi a cero; `Sparks` en Random Non-Uniform, `Alignment=VelocityAligned`, `FacingMode=FaceCamera`, estiramiento por velocidad casi nulo (`Max Scale Factor` 1.0/1.6) para que queden redondas; cono 68→120°. Tamaños actuales (en iteración con el usuario): Sparks 3–5 × 3.6–6 cm, Embers 2.5–5.5 cm.
- Spawns restaurados en `Event Hit` (siempre, sin filtro PM_Metal) y en overlap de `BP_Bullet_AK47` y `BP_Bullet1`. El usuario NO quería quitarlas: "elimina esas chispas" se refería al aspecto (cubo/cuadrados), no al efecto.
- **Iteración "se ven planas" (2026-09-11)**: `M_Spark` pasó a *sphere impostor* (52 nodos): bola en el 60% central del sprite con normal falsa `nz = sqrt(1-(r/0.6)²)`, sombreado `0.35+0.65·nz` (borde oscuro = volumen), núcleo amarillo `nz³ × ParticleColor.G × 1.5`, brillo especular fijo arriba-izquierda y halo rojo exterior `(1-r)^2.5 × 0.4`. WPO de 4cm hacia la cámara para que no se corten contra la pared. **`CameraVector` no existe en el vertex shader** → usar `normalize(CameraPositionWS - WorldPosition)`.
- Para que sean círculos perfectos: `Sparks` en Random Uniform (`NewEnumerator1`) 8–13cm, `Alignment=Unaligned`, módulo `ScaleSpriteSizeBySpeed` desactivado. `Embers` 5–10cm. Núcleo amarillo moderado: curvas con G 3.5→2→0.6→0.1 (el halo sigue rojo porque el tinte del material aplasta el verde a ×0.08).
- **Chispas que caen y se quedan brillando en el piso (2026-09-11)**: la causa real del viejo bug "se quedan paradas en el aire y luego caen" era el **orden de módulos**: `Collision` estaba después de `SolveForcesAndVelocity` en ambos emisores, y en `Embers` también `GravityForce` y `Drag` → la gravedad y la colisión no se integraban bien. Arreglado con `cfa fix_niagara_stack_issue --all=true` (siempre correr `validate_niagara_stack` tras tocar módulos). Además el `Rest State` de la colisión se activaba en cualquier superficie (paredes incluidas) → `Enable Rest State=false`; las opciones de reposo por normal (`Control Rest State Via Surface Normal`) están ocultas en esta versión del módulo y no se pueden setear.
- Colisión reactivada en ambos: `Restitution` 0.2, `Friction` 0.7, `Static Friction` 0.9, `Particle Radius Scale` 0.3. Nacen 3cm separadas de la pared (`Position Offset` [3,0,0] en espacio Local = a lo largo de la normal del impacto). Vida 1–4s. Curvas con brillo sostenido hasta ~65% de la vida y apagado en el último tramo. Tamaños: Sparks 4–7cm, Embers 3–6cm.

## CQB "demasiado iluminado": era la auto-exposición, no las luces (2026-09-11)
- Medido en PIE con `ShowFlag.VisualizeHDR 1`: EV medio de la escena **−2.17**, 0.28 lux en el suelo (casi a oscuras físicamente). La bodycam fuerza auto-exposición por histograma con rango `Bodycam.ExposureMinEV`=−5 … `ExposureMaxEV`=5, así que amplificaba la escena ~16× hasta parecer un almacén bien iluminado. Por eso las bajadas de luz previas (7500→2800, 550→180, 3000→250) no se notaban: la cámara las compensaba entera.
- Fix: `Bodycam.ExposureMinEV` −5 → **−0.6** en `BP_Project7BodycamCameraManager` (no se tocaron las luces ni se guardó el nivel). Exposición adaptada pasa de −1.75 a −0.70 → ~1.1 EV (≈2.2×) más oscuro. Solo afecta escenas más oscuras que EV −0.6 (zonas oscuras de cualquier mapa); escenas más claras no cambian.
- Para iterar en caliente sin guardar: `bodycam.exposure <bias> <minEV> <maxEV>` en consola durante PIE (cvar cheat, se lee cada frame). Vaciarla después (`bodycam.exposure ""`), si no pisa los valores del BP en la siguiente sesión. Más oscuro → subir minEV (p.ej. −0.2); más claro → bajarlo.

## Packs de Fab solo para 5.7 → traerlos a este proyecto 5.8 (2026-09-11)
- Fab solo ofrece "Add to Project" para proyectos de la versión del pack, y MyProject7 es 5.8, así que no aparece. Procedimiento: añadirlo a un proyecto 5.7 (el usuario usa `C:\Users\grego\Desktop\gregooo`, uno de los 3 `SimpleBodycam.uproject`; los de `D:\Unreal Projects\qwe` y `SimpleBodycamT` no se usan) y copiar la carpeta raíz del pack a `MyProject7\Content\` tal cual (misma ruta relativa = mismos package paths). Luego `AssetRegistry.scan_paths_synchronous([...], True)` y comprobar dependencias externas faltantes + `get_niagara_system_errors`. 5.7→5.8 carga sin conversión.
- Pack de chispas CGHOW copiado a `/Game/Sparks_Embers` (51 assets, 131MB): 10 Niagara, 0 errores en 5.8. Solo `L_Effects` (mapa demo) referencia `/Game/My_Manny`, que no existe — inofensivo. Ráfaga única (sirve para impactos): `NS_Sparks01` (martillazo). Chorro continuo (sierra/amoladora/fogata): `NS_Sparks02/03/04/06`, `NS_Embers01/02`. Mixtos: `NS_Sparks05/07/08`.
- **Crash del editor (2026-09-11 05:21 UTC)**: `set_niagara_stack_value` abre el editor de Niagara para manipular el stack; lanzado mientras el usuario estaba en PIE → `EXCEPTION_ACCESS_VIOLATION` en `UnrealEd`/`ToolMenus`/`LevelEditor` (refresco de menús). **Antes de abrir editores de assets, editar stacks de Niagara o cambiar de nivel, comprobar `UnrealEditorSubsystem.get_game_world()`**; si hay PIE, esperar a que el usuario salga. `open_level` también falla durante PIE.
- Mapa demo `/Game/Sparks_Embers/Maps/L_Effects` (etiquetas del propio pack): `NS_Embers01` Campfire, `NS_Embers02` Magical/Alchemical Brazier, `NS_Sparks02` Welding Torch, `NS_Sparks03` Grinding Wheel, `NS_Sparks04` Industrial Saw, `NS_Sparks05` Electrical Short Circuit, `NS_Sparks06` Space Capsule Re-entry, **`NS_Sparks07` Multiple Bullet Hit on Wall** (hecho para balas), `NS_Sparks08` Generic, `NS_Sparks01` = martillo/herrería (emisor `Hammer` con malla `SM_Hammer` + bucle Infinite; copia sin martillo y en Once en `/Game/BODYCAM_VFX/FX/NS_BulletImpact_Sparks`, pendiente de revisar si guardó el cambio de eje).

## Chispas de impacto reemplazadas por las del pack CGHOW (2026-09-11)
- Por petición del usuario ("no la crees tú, duplica esa") se eliminó `NS_Impact_Sparks_Metal` + mi `M_Spark` y las balas usan ahora **`/Game/BODYCAM_VFX/FX/NS_BulletHit_Sparks07`** = copia de `NS_Sparks07` ("Multiple Bullet Hit on Wall"). Referencias cambiadas con `replace_references` (AK×2, pistola×2, escopeta×1; todas compilan 0/0). Las entradas anteriores sobre `M_Spark`/sphere-impostor/colisión en el piso ya **no aplican** (esos assets no existen). Respaldo de los .uasset en `Saved/DeletedAssets_20260911/` (y `NS_Impact_Sparks_Metal` también está en git).
- Cambios mínimos sobre la copia (el resto es el look original del pack): emisor `source` **desactivado** (generaba impactos aleatorios 5/s en un área de 3×3m vía Death Events), `Sparks_Burst001` `Loop Behavior` Infinite→**Once** (si no, cada bala deja un efecto repitiéndose para siempre), `Cone Axis` [-1,0,0]→**[1,0,0]** (las balas rotan el sistema con `MakeRotFromX(HitNormal)`; con −X salían hacia dentro de la pared), `User.Spawn Count` 100→**200** ("el doble al impactar"). Ajustes rápidos por parámetros de usuario: `User.Life` 0.6, `User.Size` 2, `User.Color` (100,20.3,0.54), `User.Spread_Angle` 50, `User.Velocity_Multiplier` 1.
- Lección de borrado: `delete_asset` puede "purgar" el objeto y dejar el .uasset en disco si el paquete estuvo cargado/editado en la sesión (vuelve al reiniciar). Tras borrar, comprobar el disco; si quedó el archivo y el asset ya no está registrado, moverlo fuera de `Content`.

## Fogonazo del Niagara Examples Pack en todas las armas (2026-09-11)
- Pack `/Game/NiagaraExamples` (669 archivos, instalado directamente en este proyecto). Su único fogonazo es `FX_Weapons/MuzzleFlashes/NS_MuzzleFlash` (6 emisores: Flash_Center con luz, MuzzleFlash_Front, MuzzleFlash_Side, Muzzle_Sparks, Bullet_Shell_Eject con malla de casquillo, Muzzle_Smoke; eje +X adelante; `User.Bullet Shell Ofs` (-50,0,5)). Copia idéntica para FP en `/Game/BODYCAM_VFX/FX/NS_MuzzleFlash_FP` (para ajustar escala/offset sin tocar el pack). **Ojo**: los emisores muestran `Loop Behavior=0 (Infinite)` pero tienen `Life Cycle Mode=0 = System` → obedecen al `SystemState` del sistema, que ya es `1 = Once` con `Loop Duration` 0.15s. Leer el bucle del sistema con `get_niagara_stack_value --module-name SystemState --script-usage SystemUpdate --input-name "Loop Behavior"` (sin `--emitter-name`).
- Insertado en `BP_Player` → `Fire (LMB)` → `Fire Event` → composite **`Spawn Bullet`**: `Line Trace By Channel.then → SpawnSystemAtLocation(NS_MuzzleFlash_FP) → Branch` (una vez por disparo, antes del `For Loop` de perdigones de la escopeta). Location = `SelectVector(MuzAim.WorldLocation, MuzHip.WorldLocation, Aiming)` (boca real, SIN `ProjectileOffset`, que es lo que desplazaba el antiguo "fuego a la izquierda"), Rotation = `GetCameraRotation`. Nodos `A22373F9…` (fx) y `850D011F…` (select). `build_blueprint_graph` acepta `"graph_name": "Spawn Bullet"` para composites; `analyze_blueprint_graph` no los encuentra pero `export_graph --graph "Spawn Bullet"` sí (con conexiones).
- Pendiente: T7Pistol_MuzHip/MuzAim siguen en (0,0,0) → su fogonazo puede salir desplazado. Limpieza pendiente con el editor cerrado: 14 .uasset del demo de Sparks_Embers (Materials/M_Ground, MI_Ground; Meshes/SM_* salvo SM_Hammer; Textures/T_Grid*) quedaron bloqueados por el editor; moverlos a `Saved/DeletedAssets_20260911/`.

## Linterna real en cada arma con la F (2026-09-11)
- Antes: `F.Pressed → ToggleLinterna` creaba en runtime un `SpotLight` con `AddComponent` sobre Self (colgaba de la cápsula = "pegado al cuerpo"), guardado en la variable `Linterna` (borrada).
- Ahora: 4 `SpotLightComponent` de diseño, hijos de cada malla de arma: `Flashlight_AK74M` (AK74M_Arms, loc 0,56,11), `Flashlight_FN502` (FN502_Arms, 0.1,67,23), `Flashlight_Benelli` (Benelli_Arms, -5,92,22 — calculado desde `Benelli_MuzHip`, que cuelga de la cápsula), `Flashlight_T7Pistol` (T7Pistol_Arms, 70,3,6 — estimado por bounds, la T7 no tiene boca calibrada). Rotación yaw **89** en AK/FN502/Benelli (sus mallas van a yaw −89 respecto a la cámara → el cañón es su +Y) y 0 en la T7 (su malla no está rotada → cañón +X). Todas un poco por delante de la boca para que el arma no tape la luz. 1200 cd (`IntensityUnits=Candelas` puesto ANTES que `Intensity`), cono 7°/13° (círculo nítido con borde suave), 3500cm, 6500K, sombras on, `bVisible=false` por defecto.
- Índices de `FPWeaponIndex` (constantes de `EquipFP`): **Benelli=1, AK74M=2, FN502=3, T7Pistol=4** (default 3). Nueva función `UpdateLinterna`: `SetVisibility(Flashlight_X, LinternaOn AND FPWeaponIndex==k)` para las 4. `ToggleLinterna` = PlaySound2D → `LinternaOn = NOT LinternaOn` → `UpdateLinterna`. `EquipFP` llama a `UpdateLinterna` tras el último `SetHiddenInGame` (antes de `Set UsingHK416`), así la linterna pasa al arma nueva si estaba encendida. Las armas se ocultan con `SetHiddenInGame` sin propagar a hijos, por eso no se usó la visibilidad heredada.
- Ajustes rápidos: brillo = `Intensity` de las 4, tamaño del círculo = `InnerConeAngle`/`OuterConeAngle`, alcance = `AttenuationRadius`.

## Punto láser (tecla K) que "no se veía" (2026-09-11)
- El láser SÍ estaba en la K (`IMC_Player`: K → `IA_LaserToggle`, trigger Pressed → `LaserOn = NOT LaserOn`). **Ojo**: leer `imc.get_editor_property("mappings")` desde Python en UE 5.8 devuelve 0 mapeos en todos los IMC (los datos viven en otra propiedad) → usar `cfa get_input_mapping_context --asset-path ...`.
- Lógica (Tick → Sequence then_2): Branch(LaserOn) → LineTrace desde `FirstPersonCamera` hacia delante → si hay impacto, `LaserDot` en `Location + Normal*k` y visible; si no hay impacto o LaserOn=false, `SetVisibility(false)` (el else ya estaba conectado). No hay línea/rayo y el usuario NO la quiere ("déjalo como un punto rojo").
- Causa de que no se viera: `LaserDot` = esfera de 2cm fija (1–2 px a pocos metros, invisible con la exposición oscura). Fix: `M_LaserDot` (unlit opaco) con emisivo constante (60,1.2,0.6) en vez de un VectorParameter de valor desconocido; tamaño según distancia tras mostrarlo: `SetWorldScale3D(LaserDot, clamp(BreakHitResult.Distance × 0.00007, 0.02, 0.25))` (esfera base 100cm → ~2cm cerca, 7cm a 10m, tope 25cm). `LaserDot` pasado a `NoCollision` (tenía BlockAllDynamic delante de la pared). Más grande/pequeño: cambiar el 0.00007 y los límites del clamp.
- Lección: un `cfa` que responde "No Unreal editor bridge answered" puede haberse ejecutado igual en el editor → antes de reintentar un build, comprobar si ya existen los nodos para no duplicar.

## Selector de mapas con tarjetas grises (2026-09-11)
- Síntoma: tarjetas CQB/Backrooms y botón VOLVER como bloques grises + barra gris vertical en la tarjeta de Backrooms. Causa: los cambios de `WBP_MapSelect` del 2026-09-10 (botones transparentes, tarjetas anchas, imágenes asignadas) **nunca se guardaron** (el .uasset en disco era de las 20:23 y los cambios fueron posteriores) y se perdieron con el crash del editor. Solo quedaron las sombras inferiores anchas → hueco 0.490–0.510 sin sombra = la barra gris. **Tras editar un widget, comprobar la fecha del .uasset en disco.**
- Rehecho: `CardImg_CQB`/`CardImg_Backrooms` con `Brush.ResourceObject` = `/Game/UI/MapThumbnails/T_CQBWoodCard` / `T_BackroomsCard` (1024×882, capturas reales de los mapas; el usuario pidió usar esas y NO recapturar ni cambiar de mapa). Texturas pasadas a `TEXTUREGROUP_UI`, `NeverStream`, `TC_EDITOR_ICON` (mientras compilan reportan 32×32; las dimensiones reales están en el tag `Dimensions` del asset registry).
- Diseño ancho: grupo CQB X 0.055–0.490, grupo Backrooms X 0.510–0.945 (imagen, `Shade_*`, botón y textos), Y 0.225–0.890; `Shade_CQB`/`Shade_Backrooms` alfa 0.38→0.15. Botones de tarjeta: Normal `NoDrawType`, Hovered Box rojo α0.22, Pressed α0.38. `Btn_Back`: Normal Box negro α0.7, Hovered rojo α0.9, Pressed rojo oscuro.

## Tercer mapa en el selector: Abandoned Courtyard (2026-09-11)
- `/Game/Maps/Abandoned_Courtyard` (edificio de ladrillo en U con patio; tiene `AC_PlayerStart`, sin override de GameMode → usa el global `GM_FPS`, correcto). Miniatura `/Game/UI/MapThumbnails/T_CourtyardCard` capturada desde la vista aérea del editor con `LevelEditorSubsystem.editor_set_game_view(True)` + `select_nothing()` (se restauran después), recortada a 1024×882.
- `WBP_MapSelect`: duplicados de la tarjeta de Backrooms → `CardImg_Courtyard`, `Shade_Courtyard`, `Shade_Courtyard_Bottom`, `Btn_Courtyard`, `Txt_Courtyard_Index/Title/Desc` ("03 // URBANO", "ABANDONED COURTYARD", "EXTERIOR · PATIO EN U · COMBATE URBANO"). `On Clicked (Btn_Courtyard)` → SetInputMode_GameOnly → bShowMouseCursor=false → `OpenLevel("/Game/Maps/Abandoned_Courtyard", absolute)`, igual que las otras dos.
- Layout 3 en fila: X 0.055–0.338 / 0.3585–0.6415 / 0.662–0.945, Y 0.25–0.82 (franja de texto 0.69–0.82). Las tarjetas son más altas que las fotos (1.16) → `Brush.UVRegion` X 0.09–0.91 en las tres para no deformar (campo del Box2f = `bIsValid`, no `IsValid`). **Al escribir `Brush` hay que pasar el pincel completo** (ResourceObject, ImageSize, DrawAs, UVRegion) para no perder campos.
- `unreal.WidgetBlueprintLibrary` no existe en el Python de este editor; `open_level`/`play_in_editor` fallan si el usuario ya está en PIE.

## Láser y balas salen del cañón (no de la cámara) (2026-09-11)
- Síntoma: láser "lento" y desviado a la izquierda; balas también. Causa: ambos trazaban desde `GetPlayerCameraManager → GetCameraLocation/Rotation` (POV del bodycam, con suavizado/retardo), mientras el usuario había girado el AK con ChatGPT **en el Tick** (`Set Relative Rotation` de `AK74M_Arms` → RInterpTo a `SelectRotator(A=(0,-84,0) apuntando, B=(3,-82,0) cadera)`, y `SelectVector(A=(8,-3.5,-22), B=(8,-1,-35))` de posición) → el cañón ya no apunta al centro de la cámara.
- Fix: las 8 bocas (`*_MuzHip`/`*_MuzAim`) orientadas con su +X a lo largo del cañón (yaw 89 en AK/FN502/Benelli, cuyas mallas llevan el cañón en +Y; yaw 0 en la T7). `T7Pistol_MuzHip/MuzAim` (antes 0,0,0) → (66,3,6) estimado por bounds. `Benelli_MuzHip/MuzAim` re-colgadas de `Benelli_Arms` (antes de la cápsula: no seguían el pitch) en (-5.3,87.1,23.8). Como son hijas del arma, siguen cualquier giro del Tick.
- Láser (Tick, then_2): LineTrace Start = `SelectVector(MuzAim.loc, MuzHip.loc, Aiming)`, End = Start + `SelectVector(MuzAim.fwd, MuzHip.fwd, Aiming)` × N. Balas (`Spawn Bullet`): el mismo origen/dirección en su `Line Trace By Channel`; `FindLookAtRotation(spawn → ImpactPoint)` ya existente hace el resto. Fogonazo: Rotation = `MakeRotFromX(fwd del cañón)`. Los nodos de cámara antiguos quedaron huérfanos (puros, sin efecto).
- **Ajuste apuntando (2026-09-11)**: el usuario también giró la FN502 (tecla 2 = índice 3; teclas: 1→AK idx2, 2→FN502 idx3, 3→Benelli idx1, 4→T7 idx4). Con el láser por el eje del cañón, en ADS el punto quedaba arriba-izquierda de la mira (el roll 3° de `FN502_Arms` inclina el eje +Y de la malla y la boca va desplazada). Solución híbrida en láser y `Spawn Bullet`: **apuntando** → origen/dirección = `FirstPersonCamera` (componente, no el POV del camera manager → sin retraso; las poses ADS del Tick centran la mira ahí); **cadera** → `MuzHip` (eje del cañón). El fogonazo sigue naciendo en la boca y su rotación sigue la dirección elegida.
- **Revertido (2026-09-11, a petición)**: el ajuste híbrido "apuntando = FirstPersonCamera" se quitó entero; láser y balas vuelven a `MuzAim` (ADS) / `MuzHip` (cadera) a lo largo del cañón. BP_Player quedó con el mismo tamaño en bytes que antes del cambio (2.717.921).

## Francotirador de cerrojo en la tecla 5 (2026-09-11)
- Pack `fps-animations-sniper-rifle.zip` (mismo autor que la FN502): `source/BoltActionRifle.fbx` importa bien por CLI con la receta FN502 (`AssetImportTask`+`FbxImportUI` mínimo) → `/Game/Anims/FPS_Sniper/Raw`: mesh `BoltActionRifle` (52 huesos: Body/Bolt/Mag/Trigger + brazos), `BoltActionRifle_Skeleton`, anims `BoltActionRifleRig_SRifle_{Idle 3.0s, Walk 1.25s, Shot_nosight 1.67s, Shot_sight, Reload 3.17s, Reload_Full 4.75s}`. Borradas las basura (`FPS_Pistol_Fire*`, `SunAction`) y los 3 MIC autogenerados (texturas placeholder).
- Materiales: `M_Sniper_Rifle` / `M_Sniper_Other` (Albedo Color; Metallic/Roughness/AO con sRGB off + `TC_MASKS` → sampler Masks canal R; Normal `TC_NORMALMAP`). Slot `arms` → `M_FN502_Arms` (las texturas de brazo del pack son idénticas byte a byte a las de la FN502). Asignados en el propio SkeletalMesh (sí se guardó, verificado leyendo de vuelta).
- `ABP_FPS_Sniper` clonado de `ABP_FPS_FN502` con `export_graph` → sustituir rutas en el JSON → `build_graph --clear-graph` (EventGraph y AnimGraph con la máquina entera). Umbral IsFiring 0.2 → 1.6 s (para que se vea el cerrojo), Fire con `bAlwaysResetOnEntry=true` (vía Python: `find_graph(bp,"Fire").get_outer()`), Fire/Reload sin loop. OJO: `build_graph` pone nombres genéricos a los estados ("State", "State_1"…) → renombrados con `BlueprintEditorLibrary.rename_graph`. El aviso "TryGetPawnOwner does not exist" del build es falso: compila 0/0.
- Colocación sin adivinar: el FBX trae la cámara del autor; el mapeo FBX (Y-up) → malla UE es (x, z, y). Cámara del autor en la malla = (0, −9.29, 24.08) mirando +Y. El eje del cañón se sacó del recorrido del hueso `Bolt` en `Shot_nosight` (se desliza en −Y puro, Z constante → cañón nivelado, pasa por (−6.05, 10.78, 18.62)); las esquinas de la caja/cápsula del hueso Body daban ±7° de error, no usarlas. `Sniper_Arms` (hijo de FirstPersonCamera) rot (0,−89,0), cadera (9.29, 0.16, −24.08) = cámara del autor sobre nuestra cámara. ADS (8.23, −6.24, −24.96) = rifle centrado con el ojo 6.3 cm sobre el cañón, como en la pose `Shot_sight`. `Sniper_MuzHip/MuzAim` (−5.1, 74.5, 18.6) yaw 89; `Flashlight_Sniper` (−5, 80, 17) con los valores de las otras linternas.
- BP_Player: `IA_Weapon5` (ya existía sin uso) + tecla `Five` en IMC_Player → `EquipFP(5)`; EquipFP: `SetHiddenInGame(Sniper_Arms, Index≠5)` tras el de la T7 y `Branch(==5)` → MuzHip/MuzAim del sniper → LoadFPWeaponStats; Tick: kick con decaimiento (VInterpTo a 0, vel. 15) + ADS (VInterpTo 24) antes del Sequence; Fire Event: `Branch(==5)` → `Sniper_KickOffset=(−10,0,4)`; UpdateLinterna: 5.ª `SetVisibility`. DT_FPWeapons fila 5 `Sniper`: FireDelay 1.7, semi, MagSize 5, recoil 2.5/0.3/6, bala y sonido del AK. Compila 0/0; verificado en PIE (CQB_Wood_Houses): el rifle aparece con visor y brazos.
- Pendiente: la recarga del sniper dura 3.17 s y el Delay global de recarga es 2.85 s (se corta el final con el crossfade de 0.15 s).
- **Visor con lente real (PiP) y retroceso fuerte (2026-09-11)**: "la mira no funciona" → el visor del modelo era solo geometría. Solución tipo Tarkov: `RT_SniperScope` (RenderTarget 1024², RGBA16f) + `Sniper_Capture` (SceneCaptureComponent2D, FOV 8° ≈ 4 aumentos, `SCS_SceneColorHDR` para que el tonemapper principal lo trate como el resto, `HideComponent(Sniper_Arms)` en EquipFP para que no se vea el propio cañón/supresor) colgada del hueso `Body` en el objetivo, y `Sniper_Lens` (Plane de 3.2 cm, `M_SniperLens` Unlit/Masked: RT × viñeta SmoothStep(0.30-0.47) × retícula en cruz por `If(|uv-0.5|<0.0025)`, máscara circular r<0.5) 0.24 cm delante del ocular. Ambos solo activos al apuntar (Tick: `SetVisibility` + `SetCaptureEveryFrame` = `Aiming AND Index==5`). Geometría del visor sacada de los vértices del FBX (mapeo Blender→UE = (100x, −100y, 100z), comprobado contra los bounds): eje x=0, z=14.03, ocular y=12.04 (r 1.78), objetivo y=30.12; Body-local vía `MathLibrary.make_relative_transform` con la pose **Idle** (no la ref: la Idle lleva el rifle ~6° girado y la retícula salía torcida). Orientación del plano: X = derecha de pantalla (`cross(up, eje)`), Y = abajo (V de la textura crece hacia abajo), Z = hacia el tirador; con `make_rot_from_z` a secas la imagen salía girada 180°. ADS: cámara 0.26 cm por delante de la cara del ocular → el POV real (ForwardOffset −6) queda a 5.5 cm del disco (near clip 5 cm), disco ≈ 34 % del ancho; `SelectVector` A = (−7.13, −5.61, −25.01), VInterpTo 14. Verificado en PIE haciendo `Aiming` instance-editable un momento (`set_blueprint_variable_properties --instance-editable`, luego `=false`) y `pawn.set_editor_property("Aiming", True)`: la consola `set BP_Player_C Aiming 1` NO funciona. Bocas `Sniper_MuzHip/MuzAim` recolocadas en la boca real (−4.93, 69.73, 17.87) yaw 88.84 (el eje del visor), linterna (−4.9, 76, 17). Retroceso: DT `RecoilPitch 4.5°`, `RecoilYaw 0.8`, `RecoilRecovery 8`; `Sniper_KickOffset` = `SelectVector(Aiming: A=(−3,0,1), B=(−12,0,4))` en Fire Event (en ADS menos, porque la anim ya echa el rifle 5 cm atrás y el ocular pasaría por dentro de la cámara). Descartado: `Bodycam.ADSFOV` por arma — el struct `FBodycamCameraSettings` es solo `EditAnywhere` (sin `BlueprintReadWrite`), el nodo "Set members" no expone ningún pin; solo se puede con C++.

- 2026-09-11 — Saiga-9 en la tecla 6 sin rig propio: brazos del AK duplicados sin arma (`SK_AKArms_NoWeapon`, material `OpacityMask=0` en `ak74m`), cuerpo/cargador fusionados con `merge_static_mesh_actors` y colgados de los huesos `PBody`/`Pmag`/`Pmag2` (escala ×0.01), alineados por la línea del cañón del AK (s=0.337, yaw 90, t=(0,28.7,−30.64)); `SetHiddenInGame` necesita `bPropagateToChildren=true` o el arma sale en todas las teclas. PiP de mira por SceneCapture descartado: con dos RT de 1024² el editor crasheó por "Out of video memory" (la del sniper se bajó a 512).
- 2026-09-11 — Sybyr en la tecla 7: mismo método sobre los brazos de la FN502 (`SK_FN502Arms_NoWeapon`, huesos `Main`/`Slide`/`Mag`, s=1.28, yaw 90, t=(0,−3.60,−1.37)); reload reutiliza el de la FN502. Láser en G solo con `FPWeaponIndex==7` (`LaserSybyrOn`); teclas 2 y 4 desmapeadas en `IMC_Player`. Retroceso de esta arma mueve el arma (`Sybyr_KickOffset` + `Sybyr_KickRoll`), no los brazos.
- 2026-09-11 — Retroceso único por arma en tres capas: DT (`RecoilPitch/Yaw/Recovery` → AddControllerPitchInput en "Optional Recoil Sequencer"), `X_KickOffset` por arma (Fire Event + decaimiento VInterpTo en Tick) y `AddShotImpulse` del camera manager (C++ recorta Strength 2.5 / pitch −2°). Fogonazo quitado de todas las armas. Sonido `0911.mp4` → `SC_Fire_0911` (AK y Saiga); `SC_Fire_Sniper` para el francotirador.
- 2026-09-11 — Enemigos: 12 `BP_ShooterNPC` + `NavZonaCombate` + RecastNavMesh en `CQB_Wood_Houses` (¡`open_level --level-path` falla en silencio y deja el mapa anterior; usar `LevelEditorSubsystem.load_level()` y comprobar el nombre del mundo!). `AShooterNPC::TakeDamage` no llama a `Super`, así que `AnyDamage` de BP nunca salta: la reacción al impacto se hace en el Tick de `BP_ShooterNPC` comparando `CurrentHP` con `VidaAnterior` → `LaunchCharacter(atrás×230 + Z 150)`. `Die()` ya hace ragdoll.
- 2026-09-11 — HUD bodycam `WBP_BodycamHUD` (`/Game/UI`): CanvasPanel con anclas por esquina (DroidSansMono, sin cajas): `Txt_Fecha`+`Txt_Marca` arriba-izq, `Txt_REC`+`Img_RecDot` arriba-der (anim `Anim_Rec` opacidad 1→0.15→1 en 1 s, bucle), `Txt_Arma`+`Txt_Municion` abajo-der. Sin Tick: `Event Construct` → `SetTimerByFunctionName("ActualizarHora", 1 s, loop)` (`Now` → `ToText(int)` con `MinimumIntegralDigits` 4/2 y `bUseGrouping=false`, si no el año sale "2,026") → `Format "{y}-{mo}-{d} {h}:{mi}:{s}"`. Enganche en `BP_Player` sin tocar armas: variables `BodycamHUD` y `NombreArmaActual`, función `ActualizarBodycam` (IsValid → `ActualizarArma(NombreArmaActual)` + `ActualizarMunicion(Trunc(Ammo), Ammo>0?1:0)`), creada en "Set Up UI" tras el HUD viejo (que queda `Collapsed`) e insertada con `insert_node_in_exec` tras "Adjust Ammo Count" (Fire Event), tras los SetText de "Reload (R)" y "Delay for Animation", y al final de `UpdateFPWeaponHUD` (que además guarda `NombreArmaActual`). `Ammo` es float → `Truncate`. Los grafos colapsados se editan con `--graph-name "Set Up UI"` etc. y `export_graph --asset X --graph "Nombre"`.
- 2026-09-11 — Enemigos de un solo tiro: `CurrentHP` (C++ EditAnywhere) puesto a 10 en los defaults de `BP_ShooterNPC` (y `VidaAnterior` 10); el daño mínimo del juego es el perdigón (12), así que cualquier bala mata y `Die()` hace el ragdoll. Verificado en PIE con ApplyPointDamage 12 → HP −2 y física activa.
- 2026-09-11 — Luces de `CQB_Wood_Houses`: los 12 tubos (2 Cubes: `Steel_fixture`+`Lamp_diffuser`) y sus RectLight 2800/5800K se sustituyeron por lámparas industriales colgantes hechas con BasicShapes (cable Cylinder 130 cm desde el techo z=1105, campana Cone 110×45 cm con `M_Campana_Metal`, bombilla Sphere con `M_Bombilla_Calida` emisivo ×12) + SpotLight 2200 cd a 3000K (cono 35/68°, att 2600, Stationary). CeilingFill 250→120 e Interior 180→140, todas a 3400K: el mapa dejó de verse blanco lavado. **REVERTIDO a petición del usuario** ("deja la luz como estaba"): se borraron las 48 piezas `Lampara_*` y se recrearon los 12 RectLight + 24 Cubes con los valores originales. OJO: en `execute_python` las funciones def no ven las variables globales del script (NameError) → escribir bucles planos.
- 2026-09-11 — Impactos en madera: función de biblioteca `/Game/BODYCAM_VFX/BFL_Impactos::ImpactoBala(Hit, Direccion)` (los inputs struct se declaran con `--param-type HitResult --param-kind struct`; con `--param-type struct --type-path X` salen como "Struct Object Reference" y nada conecta). Hace: decal `M_Impact_WoodBurn` (DeferredDecal translucent; agujero negro + halo quemado con Noise por WorldPosition, 8×9×9, 90 s) orientado con `MakeRotFromX(−ImpactNormal)`, humo negro `NS_Impact_WoodBurn` (copia de `NS_Impact_Wood` con Sparks/SecondarySparks/LightDecal apagados, Dust `ScaleColor.Scale RGB`=0.05, vida 1.4–2.8 s, 22 partículas; los módulos se nombran por `name` interno: `ScaleColor`, `InitializeParticle`, `SpawnBurst_Instantaneous`), y si `PhysMat != PM_Metal` traza de vuelta desde `ImpactPoint + Dir×70` hasta `ImpactPoint + Dir×1.5`: si golpea al mismo actor pone decal de salida (8×12×12) + humo ×0.8. Enganchada en `BP_Bullet_AK47`/`BP_Bullet1`/`BP_Bullet_Shotgun` en Event Hit: `ApplyPointDamage → ImpactoBala(Hit, Normal(GetVelocity)) → DestroyActor`; se borraron el SpawnSystem de chispas, el Branch y el SpawnDecal viejos. Verificado en PIE llamando `cdo.call_method('ImpactoBala', args=(pawn, hit, dir))` (el `__WorldContext` oculto se pasa como primer argumento): decals quemados visibles en OSB y la traza de salida encuentra la cara trasera (paneles de 12 cm). Los defaults de objeto (`DecalMaterial`, `SystemTemplate`, `B` de NotEqual) hay que ponerlos con `set_pin_default` tras el build, y `Multiply_VectorFloat` se promociona a `vector * vector` (B como vector). Velocidad de bala 12000 → 60000 (AK/FN502) y 45000 (escopeta) vía `set_blueprint_component_property ProjectileMovement {InitialSpeed, MaxSpeed}`.
- 2026-09-12 — Tecla 9 = Manny de cuerpo entero con el AK (prueba de "true first person", sin tocar las otras armas). Assets: `/Game/Weapons/MannyAK/SK_AK74M_NoArms` (copia de `FpsAnims` con `M_Saiga_Invisible` en el slot `arms`), `ABP_MannyPlayer` (copia de `ABP_TP_Rifle` + `Slot 'DefaultSlot'` insertado entre el Layered blend y el Output Pose con `build_blueprint_graph --graph-name AnimGraph` y `node_id` del search "DefaultSlot"), montages `MT_Manny_Fire/Reload/Equip` (`create_anim_montage`), fila `MannyAK` en `DT_FPWeapons` (índice 7 → `EquipFP(8)`), `IA_Weapon9` mapeada a `Nine`. En `BP_Player`: `Manny_Body` (SKM_Manny_Simple, rel (−34,0,−90) yaw −90, hidden por defecto, `HideBoneByName(head)` en BeginPlay), `Manny_AK` en el socket `HandGrip_R` escala 0,68 rel loc (−0.51,1.43,4.22) rot (P 1.10, Y 16.59, R 5.77) — calculado en PIE con `make_relative_transform(T_deseada, socket_world)` donde la deseada es mesh +Y = adelante del actor (yaw −90) y 12° de morro abajo (¡para un mesh con +Y adelante el "morro abajo" es ROLL, no pitch!), `Manny_MuzHip/MuzAim` hijos de `Manny_AK` en (0,95.8,13) (`Rif_end` NO es la boca). Lógica: EquipFP oculta `Manny_Body` con `Index != 8` (propagar), Branch `Index==8` fija las bocas y toca `Montage_Play(MT_Manny_Equip)` vía `GetAnimInstance` (¡no usar `PlayAnimation`, rompe el AnimBP!); Fire Event y Reload (R) tocan `MT_Manny_Fire`/`MT_Manny_Reload` con `Branch(FPWeaponIndex==8)`. Al borrar y recrear componentes los `Get` de esos componentes desaparecen del grafo y hay que volver a crearlos. Cámara no tocada: el cuerpo se mueve 34 cm atrás para que la cámara quede en el cuello.
- 2026-09-12 — Textos "Roaming while Searching" sobre los enemigos: son las tareas `StateTreeDebugTextTask` de `ST_Shooter` (7 estados); desactivadas con `set_statetree_node_property --target instance --property-name bEnabled false` y `compile_statetree`.
- 2026-09-12 — Armas: teclas 3 (Benelli) y 9 (Manny) desmapeadas de `IMC_Player` (los assets y la lógica siguen; basta volver a mapear `Three`/`Nine`); arma inicial = AK (`EquipFP` Index 2 en los dos nodos de "Begin Play Event", antes 1 y 3).
- 2026-09-12 — Dron FPV kamikaze en la tecla **T** (`/Game/Dron/BP_Dron`, Pawn): raíz `Colision` (Sphere 24, perfil Pawn) + malla de BasicShapes (cuerpo, batería, carga, 2 brazos, 4 motores, 4 hélices que giran en Tick a 2600°/s), `Camara` (FOV 100, pitch −8, `bUsePawnControlRotation`) con post-proceso propio (Histogram, bias 1.1, EV −1.5..5, grano 0.6, fringe 2.5, viñeta 0.55, saturación 0.55 — **necesario**: `AMyProject7CameraManager::UpdateViewTarget` sale antes si el view target no es `ACharacter`, así que el dron no hereda la exposición bodycam y se veía negro), `Foco` 700 cd sin sombras (a 6000 cd la autoexposición oscurecía todo), `Movimiento` FloatingPawnMovement (1400, turbo 2600 con Shift). Controles: WASD/ratón (`IA_Move`/`IA_Look` del propio `IMC_Player`, que sigue activo al cambiar de pawn), Espacio/Ctrl sube/baja, clic izq = `Explotar`, T = `Devolver`; choque (`OnComponentHit` de Colision, `bound_event`) explota si `Armado` (0,8 s tras poseer); timer 60 s explota. `Explotar`: NS_Explosion_Medium ×1.6, `GrenadeLauncher_Explosion_Cue`, `ApplyRadialDamageWithFalloff` 240/25 r 260–750, decal quemado 260 cm, `CS_Explosion_01` ×2.5, luego `Devolver` (quita `WBP_DronHUD`, llama `DronTerminado` del dueño, `Possess(Dueno)`, destroy). En `BP_Player`: `IA_Dron` → evento `LanzarDron` (spawn 95 cm delante de la cámara, `Dueno`=self por `Set Dueno` con `target_class`, `DronesRestantes` 3→, oculta `BodycamHUD`, `Possess`); `DronTerminado` limpia `DronActual` y reenseña el HUD. Verificado en PIE por Python (`call_method('LanzarDron')` → pawn=BP_Dron, vuelo, `Explotar` → pawn=BP_Player, restantes 2). El HUD del dron (`WBP_DronHUD`) se crea en `Event Possessed` (no en BeginPlay, porque `Dueno` se asigna después del spawn) y actualiza ALT/DIST/BAT cada 0,1 s. Pendiente: modelo de dron de Fab, inclinación del cuerpo al volar.
- 2026-09-12 — Dron v2: (1) inclinación "pro": todo lo visual + `Camara` cuelgan de un `SceneComponent Chasis` (`reparent_component`); en Tick, velocidad en espacio local (`UnrotateVector(GetVelocity, ActorRotation)`), tasa de guiñada `(yawControl − YawAnterior)/dt` normalizada, `BankActual = FInterpTo(→ clamp(lat·0.05 + yawRate·0.32, ±90), 5)`, `PitchActual = FInterpTo(→ clamp(fwd·0.018, ±25), 5)`; `Chasis` rota (roll=Bank, pitch=−Pitch) y la cámara (con `bUsePawnControlRotation=false`) recibe solo el pitch del control como rotación relativa, así el roll del chasis inclina la vista como una FPV real. (2) "Sin señal": material UI `M_Estatica` (Noise ValueALU sobre `TexCoord×(640,360)` con `Time×41` en Z + bandas seno) en un `Overlay CapaSinSenal` (imagen a pantalla completa + "SIN SEÑAL" + "ENLACE PERDIDO") del `WBP_DronHUD`, función `SinSenal`; `Explotar` ahora: FX/daño/decal/shake → `Movimiento.Deactivate` → `Zumbido.Stop` → `SetActorHiddenInGame` → `SinSenal` → `SetTimerByFunctionName(Devolver, 1.4 s)` (las funciones no admiten Delay: timer). (3) Sonido: `S_Dron_Loop` sintetizado con Python (`wave`: 4 motores 178–190 Hz con 6 armónicos + ruido, crossfade de 50 ms para bucle), `looping` en el wave y `bLooping` en el WavePlayer de `SC_Dron_Loop`; `AudioComponent Zumbido` auto-activo y en Tick `SetPitchMultiplier(0.95 + |vel|·0.00018)`. En Git Bash `--sound-waves "/Game/..."` se convierte a ruta Windows: usar `MSYS_NO_PATHCONV=1`.
- 2026-09-12 — Luz solar en `CQB_Wood_Houses`: el techo (Cube 76.8×95 m) se ocultó (`CQB_Warehouse_Roof_VIEJO_oculto`, sin colisión, reversible) y se sustituyó por 7 paneles `Techo_Panel_NN` de 8 m con claraboyas abiertas de 3,2 m entre ellos + 8 vigas transversales `Techo_Viga_NN`; añadidos `Sol` (DirectionalLight movable 9 lux, 5400K, pitch −52 yaw 38, sol atmosférico, light shafts, volumetric scattering 2.5), `Atmosfera` (SkyAtmosphere), `Cielo` (SkyLight real-time capture) y `NieblaSolar` (ExponentialHeightFog densidad 0.012 con `enable_volumetric_fog` para los rayos). Todas las RectLight artificiales (19 que quedaban) borradas a petición del usuario. Nivel guardado.
- 2026-09-12 — Dron v3 (vuelo y sonido "pro"): `FloatingPawnMovement` 1800 (turbo 3000 con Shift), Acceleration 3400, Deceleration 1500 (más inercia/derrape), TurningBoost 8; bamboleo de vuelo estacionario en el `Chasis` (`SetRelativeLocation Z = sin(t·10.7)·1.6`) encadenado tras el ajuste de cámara del Tick. Sonido: `S_Dron_Loop` regenerado (4 motores 236–248 Hz, 14 armónicos con caída h^0.85 + silbido del 13º modulado + aire filtrado, normalizado a 0.89, crossfade 60 ms), `VolumeMultiplier` 1.9 y atenuación propia `AT_Dron` (esfera, radio interno 5 m, falloff 65 m, NaturalSound, oclusión con LPF 1200 Hz y −45 % de volumen, reverb send); el tono sigue a la velocidad (`0.9 + |vel|·0.00026`). Detonación automática al tocar cualquier cosa: además del `OnComponentHit` (ahora con `bNotifyRigidBodyCollision` y overlaps activados en `Colision`), en Tick un `SphereTraceByChannel` r=30 desde la posición hasta `pos + vel·dt·1.35` ignorando al `Dueno`: si acierta y está `Armado` → `Explotar` (así no atraviesa madera ni paredes yendo rápido).
- 2026-09-12 — Dron: velocidad bajada a 850 (turbo Shift 1500), Acceleration 1500 / Deceleration 1100; para compensar, el banking se hizo más sensible (lateral ×0.11, avance ×0.04). Sonido: tono base **1.0** en reposo (antes 0.9, sonaba grave al estar quieto) y factor 0.00042 → suena normal parado y sube al acelerar.
- 2026-09-12 — Subir/bajar del dron no funcionaba porque `IA_Jump` (Pressed+Released) e `IA_Crouch` (Pressed) solo disparan `Triggered` en el frame de la pulsación. Creadas `IA_DronSubir` e `IA_DronBajar` (Boolean, **sin triggers** → Down implícito: se dispara cada frame mientras se mantiene) mapeadas a SpaceBar y LeftControl en `IMC_Player`, y el dron ahora usa esas; los eventos IA_Jump/IA_Crouch se borraron de su grafo. La misma tecla en dos acciones no molesta: el pawn no poseído no recibe input.
- 2026-09-12 — Vuelo del dron rehecho (cinematográfico). Se borró el control directo (`AddMovementInput`/`AddControllerYawInput`/`SetRelativeRotation` por velocidad cruda) y ahora TODO pasa por estado + `FInterpTo`/`VInterpTo` con delta time: los eventos de input solo guardan **intención** (`InFwd/InRight/InUp/InYaw`, puestos a 0 en `Completed`), y en Tick: `SuavX = FInterpTo(SuavX, InX, dt, 3.2/2.6)` → `VelDeseada = (Fwd·SuavFwd + Right·SuavRight + Z·SuavUp)·MaxSpeed` → `VelActual = VInterpTo(…, 2.1)` → se escribe en `Movimiento.Velocity` (el componente conserva colisión/sweep; para que no interfiera: Acceleration 20000, **Deceleration 0**, TurningBoost 0, MaxSpeed 850 / 1500 en turbo). Giro: `YawRate = FInterpTo(YawRate, InYaw·105·MapRangeClamped(|Vel|,0..1100 → 1.0..0.42), 2.6)` e integrado con `AddActorWorldRotation(YawRate·dt)` → el giro acelera y desacelera y es más abierto a alta velocidad. Banking: `Roll = FInterpTo(clamp(YawRate·0.34 + velLateral·0.011, ±32), 3.4)` y `PitchCuerpo = FInterpTo(clamp(velAdelante·0.013, ±14), 3.0)` aplicados al `Chasis`; la cámara usa `CamPitch` acumulado del ratón (clamp −75..60) y el control rotation se sincroniza con el yaw del actor (`bUseControllerRotationYaw=false`). Verificado en PIE: acelera a 1100 en ~1 s, YawRate 51→44 °/s, roll 17→11° entrando y saliendo de la curva, y el choque detona. `InFwd/InRight/InUp/InYaw` quedaron `instance_editable` para poder probar desde Python.
- 2026-09-12 — ADS con inercia de giro (arma primero, cámara después): el efecto es el `TurnLag` de `AMyProject7CameraManager` (línea ~295: acumula el delta de rotación × `TurnInertia` × MotionScale, clamp `MaxTurnLagDegrees`, vuelve a 0 con `RotationLag`, y resta el lag al POV). En ADS se multiplicaba por `Bodycam.ADSMotionScale` = **0.12**, lo que lo dejaba en ~0.1° (imperceptible). Subido a **0.55** en `BP_Project7BodycamCameraManager` → medido en PIE: retraso de 5–8° al girar apuntando (antes ~0.2°). El arma NO tiene sway propio en Blueprint: va pegada al pawn y rota al instante con el control, por eso al retrasar la cámara parece que el arma va primero. Si el bamboleo de pasos en ADS molesta, la solución fina es C++: separar el TurnLag del MotionScale con un `ADSTurnLagScale` propio (requiere recompilar con el editor cerrado, ver memoria myproject7-cpp-build-blocked).
- 2026-09-12 — Dron v4: (1) **trayectoria vertical con el ratón**: `PitchVuelo = FInterpTo(clamp(CamPitch·0.88, ±55), 2.0)` y el vector de avance ya no es `GetActorForwardVector` sino `GetForwardVector(MakeRotator(PitchVuelo, YawActor))`, así al mirar abajo/arriba mientras se avanza el dron describe una curva descendente/ascendente suave; el morro del chasis suma `PitchVuelo·0.6` al pitch por aceleración. Space/Ctrl siguen sumando en Z (`SuavUp`). (2) **Boost progresivo con Shift**: el evento solo pone `BoostInput` 0/1; `BoostActual = FInterpTo(…, 1.5)` y `VelMax = Lerp(850, 2100, BoostActual)` (el `MaxSpeed` del componente se subió a 2400 para no recortar); el yaw pierde sensibilidad hasta 0.33 a 2100 uu/s para curvas amplias; clamps ampliados (pitch ±20, roll ±36). (3) **Sonido por carga de motor**: `MotorLoad = FInterpTo(clamp(0.22 + velNorm(0..2100→0..0.46) + |SuavUp|·0.16 + Boost·0.28 + yawRate(0..105→0..0.09)), 2.6)` → `SetPitchMultiplier(Lerp(0.80,1.60,load))` y `SetVolumeMultiplier(Lerp(0.95,2.45,load))`. Verificado en PIE: mirando −30° y avanzando baja a −257 uu/s con `PitchVuelo` −21°; con boost, vel 1672 en 1 s (boost 0.91 → 0.99), load 0.75 → 0.89, pitch de sonido 1.40 → 1.51 y volumen 2.07 → 2.29.
- 2026-09-12 — Visor del francotirador profesional. **Zoom con la rueda**: `IA_ZoomMira` (Axis1D) → `MouseWheelAxis` en `IMC_Player`; el evento solo actúa con `FPWeaponIndex==5 && Aiming`, suma el eje a `ZoomObjetivo` con `FClamp(4,20)` y **suena `SC_ScopeClick`** (clic de trinquete sintetizado con Python: golpe de ruido + resonancias 2.4/3.95/1.15 kHz, 55 ms) solo si el valor cambió (no suena en los topes). En Tick: `ZoomActual = FInterpTo(…,7)` → `Sniper_Capture.FOVAngle = 80/ZoomActual`. **Óptica**: `M_SniperLens` rehecho con un nodo `Custom` HLSL (inputs UV/Tex/EyeX/EyeY/SwayX/SwayY): distorsión de barril 0.20·r², aberración cromática por muestreo R/G/B con escalas 1.007/1/0.993, tinte de tratamiento (0.90,1.00,1.08) creciente con el radio, viñeta al borde, **eye relief** (círculo desplazado por EyeX/EyeY → sombra por el lado contrario), retícula MIL dibujada dentro del cristal (líneas de 0.0009, postes de 0.0032 entre r 0.30 y 0.448, hash marks cada 0.026, microdot) en negro casi puro, aro fino negro a partir de r=0.472 y máscara circular. Nitidez: unsharp mask de 4 muestras (×0.55) + RT a **1024²** con filtro trilineal para aguantar 20x. `MiraMID` se crea en "Begin Play Event" (`CreateDynamicMaterialInstance` sobre `Sniper_Lens`) y en Tick recibe `EyeX/EyeY` = delta suavizado (`FInterpTo` 4) entre control rotation y rotación de la cámara ×0.055 (clamp ±0.11) y `SwayX/SwayY` = ese valor ×0.42. **OJO**: `insert_node_in_exec` reasigna el pin `then` del nodo insertado, así que rompe las cadenas internas creadas en el mismo build — hay que reconectar a mano (pasó con `Set ZoomActual`→`SetFOVAngle` y con el `Set MiraMID`). El material del cuerpo del rifle (`M_Sniper_Rifle`) NO se tocó: ya es PBR con texturas propias.
- 2026-09-12 — Fisheye en la cámara de primera persona: `MI_Fisheye_FP` (instancia de `/Game/SimpleBodycam/Assets/Materials/Master/M_FishEye`, post-process after tonemapping) con Intensity 0.38 / Density 1.15 / Radius 2, añadida como **WeightedBlendable** de `BP_Player.FirstPersonCamera.PostProcessSettings` (peso 1). Sobrevive al camera manager porque `AMyProject7CameraManager::UpdateViewTarget` toma `FPostProcessSettings& PP = OutVT.POV.PostProcessSettings` por **referencia** y solo escribe campos concretos (además añade su propio blendable `BodycamLensMID` con `PP.AddBlendable`), así que no borra los blendables de la cámara. Para subir/bajar el efecto basta cambiar `Intensity` de la instancia; `Bodycam.LensDistortionStrength` (que alimenta la distorsión de borde de `PP_BodycamLens`) sigue a 0 y es un sistema aparte. Verificado en PIE: las líneas rectas del techo se curvan hacia los bordes.
- 2026-09-12 — Dron: respuesta vertical inmediata. `PitchVuelo` ahora sigue la mirada 1:1 (factor 0.88 → **1.0**), interpola a **6.5** (antes 2.0, por eso "tardaba en bajar") y admite ±70°; además el vector de velocidad recibe una componente vertical propia `sin(PitchVuelo)·0.45` sumada a `SuavUp`, así **desciende o asciende con solo mirar, sin necesidad de avanzar**. Interpolaciones de entrada más vivas: avance/lateral 4.2, vertical 4.5, velocidad 3.4.
- 2026-09-12 — Imagen "pixelada": la GPU (RTX 3060 Ti, **8 GB**) estaba a 7.4/8 GB y el streaming de texturas bajaba mips. Aplicado y fijado en `Config/DefaultEngine.ini [SystemSettings]`: `r.Lumen.HardwareRayTracing=0` (software; libera ~1 GB y en este mapa se ve igual), `r.Streaming.LimitPoolSizeToVRAM=1`, `r.Streaming.PoolSize=700`, `r.MipMapLODBias=0`, `r.AntiAliasingMethod=4` (TSR) con `r.TSR.History.ScreenPercentage=200` y `r.TSR.ShadingRejection.Flickering=1`, `r.ScreenPercentage=100`. Medido con `nvidia-smi`: 7407 → 6468 MiB tras aplicarlos en caliente. Los cambios del .ini solo entran al reiniciar el editor.
- 2026-09-12 — Fisheye ELIMINADO a petición del usuario: `FirstPersonCamera.PostProcessSettings.WeightedBlendables` vaciado (la instancia `MI_Fisheye_FP` se conserva por si se quiere volver a poner).
- 2026-09-12 — Recarga del AK: `S_AK_Reload` + `SC_AK_Reload` (`/Game/Audio/Weapons`) sintetizados con Python en 3 fases reales — retén y salida del cargador (clic 1850/3100 Hz + roce), encaje del cargador nuevo (golpe contra el pozo + CLACK 2400/3600 Hz + muelle) y palanca de armado (tirón, tope atrás, cierre fuerte con resonancia del cajón), 2,85 s, con paso bajo y normalizado. Sustituye a `SW_AK74U_Reload` en el `PlaySound2D` de la rama del AK en "Reload (R)" (nodo `4C18140F…`, volumen 1.15). La rama de la FN502 (`F5FBDC6C…`) no se tocó.
- 2026-09-12 — Explosión del dron mucho más potente: sonido propio `SC_Dron_Boom`; **regenerado el 2026-09-12 como "BUM" de bomba**: sub-grave de 62→19 Hz con caída lenta (1.05) + armónico de octava + sub a mitad de frecuencia, ataque de 5 ms, chasquido de onda de choque, rugido de fuego muy filtrado (paso bajo 0.90) y 6 ecos largos; compresor exponencial ×3.6 y wave a `compression_quality=100` para no perder el sub al comprimir; cue a 1.6 y el nodo a volumen 3.2 (5 s); FX principal cambiado a `NS_Explosion` escala 2.6 y encadenados `NS_Dirt_Explosion` (2.2), `NS_Spark_Burst` (3.0) y `NS_Smoke_Plume` (2.4); daño 400 con radios 420/1250 y `CS_Explosion_01` a escala 5. Verificado en PIE: la pantalla se llena de escombros en todas direcciones.
- 2026-09-12 — La explosión del dron vuelve al sonido original `GrenadeLauncher_Explosion_Cue` con volumen 1.6 (el usuario descartó tanto el "BUM" de sub-grave como la versión de metralla; `S_Dron_Boom`/`SC_Dron_Boom` quedan en el proyecto sin usar). Los efectos de partículas ampliados (4 capas), el daño 400 y la sacudida ×5 se mantienen.
- 2026-09-12 — Recarga del AK por CAPAS sincronizadas con la animación (sustituye al clip único). **Método para encontrar los frames de contacto**: muestrear la anim con `AnimPoseExtensions.get_anim_pose_at_time` + `get_bone_pose(..., WORLD)` de `Pmag`, `Pmag2`, `Hand_L`, `Hand_R`, `PBody` en ~55 puntos y buscar los saltos de velocidad — en `FpsAnims_AnimRig_AK_Reload` (2,65 s): la mano coge el cargador nuevo en 0.44 (la distancia mano-Pmag2 se queda fija en 13,5), el viejo se desprende en **1.18** (vel 248 → 2247), el rig hace el "salto" de huesos (= bloqueo del nuevo) en **1.89**, y la mano derecha baja/sube en 2.2–2.5 (palanca de armado). En `..._full` (3,5 s): 1.23, 1.94 y 3.0–3.4.
  13 sonidos propios en `/Game/Audio/Weapons/AK_Reload` (`S_AK_*` + `SC_AK_*`), sintetizados en Python por capas (transitorio de ruido + resonancias metálicas con decaimientos distintos + fricción filtrada + resorte): MagGrab, MagRelease, MagFriction, MagOutClack, MagKnock, MagInTuk, MagHook, MagRotate, **MagLock** (dominante, 360/640/1150/2100 Hz), BoltTouch, BoltPull, BoltRelease, BoltSettle. Cada cue lleva `SoundNodeModulator` con **pitch 0.97–1.03 y volumen 0.96–1.04** (dos recargas nunca suenan igual) y su propio `volume_multiplier` (0.30 el roce de guante → 1.00 el bloqueo). Se disparan con `AnimNotify_PlaySound` colocados con `add_anim_notify` en los tiempos medidos (13 en cada una de las dos animaciones), así van al frame del contacto y no a temporizadores. El `PlaySound2D` de la rama del AK en "Reload (R)" queda a volumen 0 (inerte) para no duplicar.
- 2026-09-12 — Boost del dron calibrado a **3×**: `VelMax = Lerp(850, 2550, BoostActual)` (MaxSpeed del componente 2700 para no recortar), palanca de gas `FInterpTo` a **7.0** y aceleración de la velocidad a **5.0** → medido en PIE: 850 (1.00×) → 2141 (2.52×) → 2550 (3.00×) de forma continua, sin saltos, en ~0.5 s. Sonido ligado: rango de `velNorm` y de la sensibilidad de giro reescalados a 2550, peso del boost en `MotorLoad` 0.32 e interpolación 5.0; tono de hélices hasta **1.78** y volumen hasta **2.7** (medido: RPM 0.62→1.00 y tono 1.40→1.78 mientras acelera). Al soltar Shift todo baja por la misma interpolación.
- 2026-09-12 — DOS fallos encontrados al probar: (1) **el sonido de recarga del AK no sonaba** porque el estado `Reload` de `ABP_FPS_AK74M` reproduce `FpsAnims_AnimRig_AK_Reload_var2`, NO `..._Reload`/`..._Reload_full` (donde había puesto los notifies). Medida la var2 (2,82 s): la mano coge el cargador nuevo en 0.26, el viejo sale entre 1.83 y 2.19, y el intercambio de huesos (bloqueo) ocurre en 2.24 → 9 notifies colocados en 0.26 / 1.78 / 1.86 / 2.06 / 2.14 / 2.19 / 2.23 / 2.27 / 2.35. (2) **el boost del dron no hacía nada con Shift** porque `IA_Sprint` solo tiene el trigger `Pressed`: `Started` y `Completed` se disparaban casi a la vez, así que `BoostInput` volvía a 0 al instante. Creada `IA_DronBoost` (Boolean sin triggers → Down) mapeada a LeftShift y conectada `Triggered`→1 / `Completed`→0; el evento `IA_Sprint` se borró del dron. Mismo patrón que `IA_DronSubir`/`IA_DronBajar`.
- 2026-09-12 — A petición del usuario la recarga del AK queda **solo con sonidos de cargador** (sin palanca de armado ni cerrojo): borrados los 4 notifies de bolt de las otras dos animaciones. `S_AK_MagLock` y `S_AK_MagOutClack` regenerados con filtro paso alto (0.985) para que sean secos y "punchy" sin bass cinematográfico.
